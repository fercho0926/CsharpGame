import type { AdvancedQuestion, AdvancedStep } from "../advanced-lessons";

// sqr rota las opciones según `shift` para que la respuesta correcta no quede
// siempre en el mismo índice (evita el patrón "la correcta siempre es A").
const sqr = (prompt: string, correct: string, wrong: [string, string, string], shift: number, why: string, code = ""): AdvancedQuestion => {
  const all = [correct, ...wrong];
  const options = all.map((_, k) => all[(k + shift) % 4]);
  const answer = (4 - shift) % 4;
  return { prompt, options, answer, why, code };
};

export const stepsN06N08: AdvancedStep[] = [
 {id:"n06-clean-architecture",title:"Clean Architecture",summary:"Clean Architecture organiza el código en capas concéntricas (Dominio, Aplicación, Infraestructura, Presentación) donde las dependencias de código solo pueden apuntar hacia adentro. El Dominio no conoce frameworks, ORMs ni UI; las capas externas implementan las abstracciones (interfaces) definidas en las internas. Esto se logra aplicando el Principio de Inversión de Dependencias: el Dominio define el contrato, la Infraestructura lo implementa.",code:`// Domain (capa más interna, sin dependencias externas)
public interface IOrderRepository
{
    Task<Order?> GetByIdAsync(int id);
    Task AddAsync(Order order);
}

// Infrastructure (capa externa, implementa la abstracción del Dominio)
public class SqlOrderRepository : IOrderRepository
{
    private readonly AppDbContext _db;
    public SqlOrderRepository(AppDbContext db) => _db = db;
    public Task<Order?> GetByIdAsync(int id) => _db.Orders.FindAsync(id).AsTask();
    public Task AddAsync(Order order) { _db.Orders.Add(order); return _db.SaveChangesAsync(); }
}`,questions:[
  sqr("¿Hacia dónde deben apuntar las dependencias de código fuente en Clean Architecture?","Siempre hacia adentro, hacia el Dominio",["Siempre hacia afuera, hacia la infraestructura","En cualquier dirección, sin restricción","Solo desde Infraestructura hacia Presentación"],0,"La 'regla de dependencia' establece que el código de una capa interna nunca puede referenciar código de una capa más externa; el flujo de dependencias siempre apunta hacia el Dominio."),
  sqr("Dado que IOrderRepository se define en el proyecto Domain y SqlOrderRepository lo implementa en el proyecto Infrastructure, ¿qué proyecto debe referenciar a cuál?","Infrastructure referencia a Domain (para implementar su interfaz)",["Domain referencia a Infrastructure","Ambos deben referenciarse mutuamente","Ninguno debe referenciar al otro"],1,"La implementación concreta vive en la capa externa y depende de la abstracción interna; el Dominio jamás debe depender de Infraestructura.",`public interface IOrderRepository { Task AddAsync(Order order); }
public class SqlOrderRepository : IOrderRepository { /* usa EF Core aquí */ }`),
  sqr("¿Qué capa contiene las entidades y reglas de negocio puras, sin dependencias de frameworks?","El Dominio",["La Presentación (controllers/UI)","La Infraestructura (acceso a datos)","La capa de configuración (appsettings)"],2,"El Dominio es el núcleo: entidades, value objects y reglas de negocio que no dependen de ningún detalle técnico externo."),
  sqr("Si el proyecto Domain agrega un PackageReference a Microsoft.EntityFrameworkCore, ¿qué ocurre con la arquitectura?","Se viola la regla de dependencia porque el Dominio pasa a depender de un detalle de infraestructura",["No pasa nada, EF Core es parte del Dominio por diseño","Mejora el rendimiento del Dominio","Es obligatorio para que el Dominio compile"],3,"El Dominio debe permanecer libre de dependencias a ORMs, frameworks web o de acceso a datos; referenciar EF Core ahí rompe la separación de capas."),
  sqr("¿Cómo logra Clean Architecture que el Dominio 'controle' el comportamiento de la Infraestructura sin depender de ella?","El Dominio define interfaces (abstracciones) que la Infraestructura implementa (inversión de dependencias)",["La Infraestructura copia el código del Dominio en tiempo de compilación","El Dominio referencia directamente las clases concretas de Infraestructura","Usando únicamente clases estáticas en el Dominio"],0,"Gracias al Principio de Inversión de Dependencias (la 'D' de SOLID), la capa interna declara el contrato y la externa lo satisface, invirtiendo la dependencia habitual."),
  sqr("¿Qué responsabilidad tiene típicamente la capa de Aplicación (casos de uso) en Clean Architecture?","Orquestar entidades del Dominio y coordinar repositorios/servicios para cumplir un caso de uso",["Definir el esquema de la base de datos","Renderizar HTML o JSON de respuesta","Configurar el pipeline de middlewares HTTP"],1,"La capa de Aplicación contiene los casos de uso: coordina el Dominio y las abstracciones de Infraestructura sin conocer detalles técnicos concretos."),
  sqr("Si el equipo decide migrar de SQL Server a PostgreSQL, ¿qué capas deberían necesitar cambios si la arquitectura respeta el dependency rule?","Solo la Infraestructura (implementaciones concretas de acceso a datos)",["El Dominio y la Aplicación también deben reescribirse","Únicamente la capa de Presentación","Todas las capas por igual"],2,"Si Dominio y Aplicación solo dependen de interfaces, cambiar el motor de base de datos afecta solamente a las implementaciones en Infraestructura."),
  sqr("¿Qué ventaja de testing ofrece Clean Architecture al usar interfaces como IOrderRepository en la capa de Aplicación?","Se puede probar la lógica de casos de uso con un mock/fake del repositorio, sin una base de datos real",["Elimina por completo la necesidad de escribir pruebas","Obliga a usar una base de datos en memoria en producción","Hace innecesarios los tests de integración"],3,"Al depender de abstracciones, los tests unitarios de Aplicación pueden sustituir IOrderRepository por un doble de prueba, sin infraestructura real."),
  sqr("¿En qué se diferencia Clean Architecture de una arquitectura en capas tradicional (UI → BLL → DAL) donde BLL llama directamente a clases concretas del DAL?","Clean Architecture invierte la dependencia: el Dominio/Aplicación define interfaces que la capa de datos implementa, en vez de depender directamente de clases concretas",["Son exactamente el mismo patrón con otro nombre","Clean Architecture no permite tener una capa de datos","La arquitectura en capas tradicional siempre es más testeable"],0,"La diferencia clave es la dirección de la dependencia: en capas tradicionales BLL depende de DAL concreto; en Clean Architecture esa dependencia se invierte mediante interfaces."),
  sqr("¿Qué capa debería depender de la capa de Aplicación, y no al revés?","La capa de Presentación (por ejemplo, controllers de una Web API)",["El Dominio","La capa de Aplicación misma","Ninguna capa debe depender de Aplicación"],1,"Presentación es una capa externa que invoca casos de uso de Aplicación; Aplicación no debe conocer detalles de HTTP, controllers ni frameworks de presentación."),
 ]},
 {id:"n06-solid-services",title:"SOLID en servicios",summary:"SOLID agrupa cinco principios (SRP, OCP, LSP, ISP, DIP) para diseñar clases y servicios mantenibles. En servicios de aplicación, aplicar SOLID evita clases con demasiadas responsabilidades, favorece interfaces pequeñas e inyección de dependencias, y permite sustituir implementaciones sin romper a los consumidores.",code:`// Antes: viola SRP (una clase con demasiadas responsabilidades)
public class OrderService
{
    public void PlaceOrder(Order order)
    {
        // valida datos, guarda en la base de datos y envía un email, todo aquí
    }
}

// Después: responsabilidades separadas e inyectadas
public class OrderService
{
    private readonly IOrderValidator _validator;
    private readonly IOrderRepository _repository;
    private readonly IEmailSender _email;

    public OrderService(IOrderValidator validator, IOrderRepository repository, IEmailSender email)
    { _validator = validator; _repository = repository; _email = email; }

    public async Task PlaceOrderAsync(Order order)
    {
        _validator.Validate(order);
        await _repository.AddAsync(order);
        await _email.SendConfirmationAsync(order);
    }
}`,questions:[
  sqr("¿Qué principio se viola en la clase 'OrderService' original, que valida, persiste y envía email en un solo método?","SRP (Single Responsibility Principle): la clase tiene más de una razón para cambiar",["OCP, porque no se puede extender","LSP, porque no hay herencia involucrada","ISP, porque la interfaz es muy pequeña"],0,"SRP indica que una clase debe tener una sola responsabilidad/razón de cambio; mezclar validación, persistencia y envío de email en un método viola ese principio.",`public void PlaceOrder(Order order) {
    // valida, guarda y envía email en el mismo método
}`),
  sqr("Tienes una clase DiscountCalculator con un switch que crece cada vez que se agrega un tipo de descuento nuevo. ¿Qué principio SOLID sugiere resolverlo con una interfaz IDiscountStrategy y nuevas implementaciones en vez de modificar el switch?","OCP (Open/Closed Principle): abierto a extensión, cerrado a modificación",["SRP, porque el switch tiene una sola responsabilidad","LSP, porque las estrategias son subtipos incompatibles","DIP, porque el switch depende de una abstracción"],1,"OCP busca que se pueda extender el comportamiento (agregar una nueva estrategia) sin modificar el código existente que ya funciona y está probado.",`public interface IDiscountStrategy { decimal Apply(decimal total); }
public class BlackFridayDiscount : IDiscountStrategy { public decimal Apply(decimal total) => total * 0.8m; }`),
  sqr("Si una clase Penguin hereda de Bird y Bird tiene un método virtual Fly(), pero Penguin lanza NotSupportedException en Fly(), ¿qué principio se está violando?","LSP (Liskov Substitution Principle): un subtipo debe poder sustituir a su tipo base sin romper el comportamiento esperado",["SRP, porque Penguin tiene demasiadas responsabilidades","ISP, porque Bird tiene pocos métodos","OCP, porque Penguin no se puede extender"],2,"LSP exige que cualquier código que use Bird funcione igual si se le pasa un Penguin; lanzar una excepción en un método heredado rompe esa sustituibilidad.",`public class Bird { public virtual void Fly() { /* volar */ } }
public class Penguin : Bird { public override void Fly() => throw new NotSupportedException(); }`),
  sqr("Una interfaz IWorker obliga a implementar Work(), Eat() y Sleep(), pero una clase RobotWorker solo necesita Work() y las otras dos las implementa lanzando NotImplementedException. ¿Qué principio sugiere dividir IWorker en interfaces más pequeñas?","ISP (Interface Segregation Principle): los clientes no deberían depender de métodos que no usan",["DIP, porque RobotWorker depende de una clase concreta","LSP, porque RobotWorker no es sustituible","SRP, porque IWorker tiene un solo método relevante"],3,"ISP recomienda interfaces pequeñas y específicas (por ejemplo IWorkable, IFeedable, ISleepable) en vez de una interfaz 'gorda' que fuerce implementaciones vacías o con excepciones.",`public interface IWorker { void Work(); void Eat(); void Sleep(); }
public class RobotWorker : IWorker
{
    public void Work() { /* trabaja */ }
    public void Eat() => throw new NotImplementedException();
    public void Sleep() => throw new NotImplementedException();
}`),
  sqr("En el OrderService refactorizado, el constructor recibe IOrderValidator, IOrderRepository e IEmailSender en vez de crear 'new SqlOrderRepository()' internamente. ¿Qué principio SOLID ejemplifica esto?","DIP (Dependency Inversion Principle): depender de abstracciones, no de implementaciones concretas",["OCP, porque el constructor no cambia nunca","LSP, porque los parámetros son polimórficos","SRP, porque el constructor solo asigna campos"],0,"DIP establece que los módulos de alto nivel no deben depender de módulos de bajo nivel concretos, sino de abstracciones (interfaces) inyectadas."),
  sqr("¿Cuál de estas afirmaciones sobre OCP (Open/Closed Principle) es correcta?","El código existente y ya probado no debería modificarse para agregar comportamiento nuevo; en su lugar se extiende con nuevas clases",["OCP exige que todas las clases sean 'sealed' para evitar extensión","OCP prohíbe usar interfaces","OCP significa que las clases nunca pueden tener métodos privados"],1,"OCP se logra típicamente mediante polimorfismo (interfaces/herencia): se añade comportamiento con nuevas implementaciones en lugar de tocar el código existente."),
  sqr("¿Qué trampa de entrevista existe sobre 'aplicar SOLID siempre a rajatabla' en servicios pequeños?","Aplicar SOLID de forma dogmática en clases triviales puede sobre-diseñar el código sin beneficio real; SOLID es una guía, no una regla absoluta en todo contexto",["SOLID es obligatorio por el compilador de C#","Sin SOLID el código no compila","SOLID solo aplica a proyectos con más de 1 millón de líneas"],2,"SOLID orienta decisiones de diseño para mantenibilidad; en servicios muy simples, aplicarlo sin criterio puede generar complejidad innecesaria (over-engineering)."),
  sqr("Con la interfaz IOrderRepository con métodos GetByIdAsync y AddAsync, ¿qué principio se aplica si se decide inyectar IOrderRepository en vez de instanciar SqlOrderRepository directamente dentro de OrderService?","DIP, porque OrderService depende de la abstracción IOrderRepository, no de la clase concreta",["ISP, porque IOrderRepository tiene solo dos métodos","LSP, porque SqlOrderRepository hereda de OrderService","SRP, porque OrderService ya no tiene responsabilidades"],3,"Inyectar la interfaz en lugar de instanciar la implementación concreta es la aplicación práctica de DIP en el contenedor de inyección de dependencias."),
  sqr("¿Qué relación hay entre DIP y la inyección de dependencias (DI) en ASP.NET Core?","DI es el mecanismo (contenedor de servicios) que permite aplicar DIP en la práctica, resolviendo automáticamente las abstracciones a sus implementaciones registradas",["Son sinónimos exactos sin ninguna diferencia","DIP prohíbe usar contenedores de DI","DI es exclusivo de proyectos que no usan SOLID"],0,"DIP es el principio de diseño; la inyección de dependencias (por ejemplo con services.AddScoped<IOrderRepository, SqlOrderRepository>()) es la técnica que lo implementa en ASP.NET Core."),
  sqr("Si IEmailSender tiene un único método SendConfirmationAsync y se usa solo para ese propósito, ¿qué principio se respeta al mantenerla pequeña y específica en vez de una interfaz genérica INotifier con 10 métodos de distintos canales?","ISP (Interface Segregation Principle)",["LSP, porque no hay jerarquía de clases","OCP, porque no se puede agregar un nuevo canal","SRP, porque el método es asíncrono"],1,"Mantener interfaces pequeñas y enfocadas evita que los consumidores dependan de métodos irrelevantes, que es justamente lo que persigue ISP."),
 ]},
 {id:"n06-repository-uow",title:"Repository y Unit of Work",summary:"El patrón Repository abstrae el acceso a datos detrás de una interfaz orientada al Dominio (GetByIdAsync, AddAsync), ocultando detalles del ORM. Unit of Work coordina varios repositorios para que sus cambios se confirmen juntos en una sola transacción (un solo SaveChanges). En EF Core, DbContext ya actúa como Unit of Work y cada DbSet<T> ya se comporta como un repositorio genérico, por lo que un Repository manual solo aporta valor si se necesita una abstracción de testing o independencia del ORM.",code:`public interface IOrderRepository { Task AddAsync(Order order); }
public interface ICustomerRepository { Task<Customer?> GetByIdAsync(int id); }

public interface IUnitOfWork
{
    IOrderRepository Orders { get; }
    ICustomerRepository Customers { get; }
    Task<int> CompleteAsync(); // hace commit de todos los cambios pendientes
}

public class EfUnitOfWork : IUnitOfWork
{
    private readonly AppDbContext _context;
    public EfUnitOfWork(AppDbContext context, IOrderRepository orders, ICustomerRepository customers)
    { _context = context; Orders = orders; Customers = customers; }
    public IOrderRepository Orders { get; }
    public ICustomerRepository Customers { get; }
    public Task<int> CompleteAsync() => _context.SaveChangesAsync();
}`,questions:[
  sqr("¿Qué responsabilidad tiene el patrón Repository?","Abstraer el acceso a datos detrás de una interfaz orientada al Dominio, ocultando el ORM o la tecnología de persistencia",["Renderizar vistas HTML","Definir rutas de un controlador","Ejecutar validaciones de entrada del usuario"],0,"Repository expone operaciones de negocio (GetByIdAsync, AddAsync) sin exponer detalles de EF Core, SQL o el proveedor concreto."),
  sqr("¿Qué problema resuelve Unit of Work cuando OrderService necesita guardar un pedido y actualizar el stock de un producto en la misma operación?","Garantiza que ambos cambios se confirmen juntos en una sola transacción (o ninguno si algo falla)",["Ejecuta cada cambio en una transacción separada automáticamente","Elimina la necesidad de manejar transacciones","Evita que dos repositorios se usen en el mismo método"],1,"Unit of Work agrupa las operaciones de múltiples repositorios y las confirma con un único Complete/SaveChanges, preservando la atomicidad."),
  sqr("En EF Core, ¿qué clase ya cumple el rol de Unit of Work por sí sola?","DbContext, mediante su método SaveChanges/SaveChangesAsync",["DbSet<T> únicamente","IQueryable<T>","DbContextOptions"],2,"DbContext rastrea los cambios de todas las entidades cargadas y los confirma juntos con SaveChanges, actuando como Unit of Work de forma nativa."),
  sqr("¿Qué crítica común existe sobre implementar un Repository genérico manual encima de DbSet<T> en EF Core sin necesidad real de abstracción?","Puede ser una capa redundante, porque DbSet<T> ya ofrece una API similar (Add, Find, Where) y EF Core ya es en sí mismo una abstracción sobre la base de datos",["Es obligatorio en todo proyecto EF Core, sin excepciones","Repository genérico siempre mejora el rendimiento de las consultas","DbSet<T> no permite hacer consultas LINQ complejas"],3,"Cuando el Repository solo delega en DbSet<T> sin agregar lógica de negocio, añade indirección sin beneficio real; su valor aparece cuando se necesita aislar el Dominio del ORM o facilitar mocks en tests."),
  sqr("¿Cuál es un motivo legítimo para seguir usando Repository explícito aun con EF Core disponible?","Aislar la capa de Dominio/Aplicación de EF Core para poder sustituir la implementación en tests unitarios con un mock/fake",["Porque EF Core no permite hacer consultas asíncronas","Porque DbContext no puede inyectarse con DI","Porque los DbSet<T> no soportan LINQ"],0,"Un Repository con interfaz permite testear la lógica de Aplicación sin una base de datos real, sustituyendo la implementación EF Core por un doble de prueba."),
  sqr("En el ejemplo, ¿qué hace exactamente IUnitOfWork.CompleteAsync()?","Llama a _context.SaveChangesAsync(), persistiendo en una sola operación todos los cambios rastreados por el DbContext, incluidos los de Orders y Customers",["Crea una nueva conexión a la base de datos por cada repositorio","Solo confirma los cambios del repositorio Orders","Revierte automáticamente cualquier cambio pendiente"],1,"Como Orders y Customers comparten el mismo DbContext inyectado, un único SaveChangesAsync confirma los cambios de ambos repositorios de forma atómica."),
  sqr("¿Qué ocurre si dos repositorios (IOrderRepository e ICustomerRepository) usan cada uno su propia instancia de DbContext en lugar de compartir una vía Unit of Work?","Sus cambios se confirmarían en transacciones separadas, perdiendo la atomicidad entre ambas operaciones",["No cambia nada, EF Core las unifica automáticamente","Se genera un error de compilación","Los cambios se revertirían automáticamente entre sí"],2,"Sin compartir el mismo DbContext/transacción, cada repositorio confirmaría sus cambios de forma independiente, rompiendo la garantía de 'todo o nada' que ofrece Unit of Work."),
  sqr("¿Qué trampa de entrevista es común sobre el patrón Repository con ORMs modernos como EF Core?","Repository fue popular para abstraer ORMs más rudimentarios; con EF Core, que ya implementa Repository+Unit of Work internamente, agregar una capa manual sin necesidad real es sobre-ingeniería",["Repository es obsoleto y prohibido en cualquier proyecto .NET moderno","EF Core no puede usarse sin implementar Repository manualmente","Repository y Unit of Work son el mismo patrón con nombres distintos"],3,"La discusión técnica real no es 'usar o no usar Repository' sino evaluar si aporta valor (testabilidad, independencia del ORM) frente a la redundancia con las abstracciones que ya ofrece EF Core."),
  sqr("¿Qué inyecta el constructor de EfUnitOfWork en el ejemplo para poder coordinar Orders y Customers?","El mismo AppDbContext junto con las implementaciones de IOrderRepository e ICustomerRepository, de modo que compartan el contexto de EF Core",["Una cadena de conexión distinta por cada repositorio","Un IServiceProvider para resolver repositorios dinámicamente","Una nueva transacción SQL manual con BeginTransaction para cada llamada"],0,"Al compartir un único AppDbContext inyectado, todos los repositorios rastrean cambios en el mismo contexto, permitiendo que CompleteAsync los confirme juntos."),
  sqr("¿Qué diferencia clave hay entre Repository y Unit of Work como patrones?","Repository abstrae el acceso a una colección de entidades específica; Unit of Work coordina y confirma los cambios de varios repositorios en una sola transacción",["Son sinónimos exactos y siempre se implementan como una sola clase","Repository maneja transacciones; Unit of Work solo hace consultas de lectura","Unit of Work reemplaza por completo la necesidad de repositorios"],1,"Repository se enfoca en el acceso a datos de una entidad; Unit of Work opera a un nivel más alto, orquestando la confirmación conjunta de múltiples repositorios."),
 ]},
 {id:"n06-cqrs",title:"CQRS",summary:"CQRS (Command Query Responsibility Segregation) separa las operaciones que modifican estado (commands) de las que solo leen datos (queries), usando modelos distintos para cada una. Un command típicamente no retorna datos de dominio (a lo sumo un id o un resultado de validación); una query nunca modifica estado. CQRS no obliga a tener bases de datos separadas para lectura y escritura: eso es una variante avanzada (a veces combinada con Event Sourcing), no un requisito del patrón base.",code:`// Command: modifica estado, no retorna datos de dominio
public record CreateOrderCommand(int CustomerId, List<OrderLine> Lines);

public class CreateOrderHandler
{
    private readonly IOrderRepository _repository;
    public CreateOrderHandler(IOrderRepository repository) => _repository = repository;

    public async Task<int> Handle(CreateOrderCommand command)
    {
        var order = new Order(command.CustomerId, command.Lines);
        await _repository.AddAsync(order);
        return order.Id; // solo el id, no el modelo completo
    }
}

// Query: solo lee, nunca modifica estado
public record GetOrderSummaryQuery(int OrderId);

public class GetOrderSummaryHandler
{
    private readonly AppDbContext _db;
    public GetOrderSummaryHandler(AppDbContext db) => _db = db;
    public Task<OrderSummaryDto?> Handle(GetOrderSummaryQuery query) =>
        _db.Orders.Where(o => o.Id == query.OrderId)
                  .Select(o => new OrderSummaryDto(o.Id, o.Total)).FirstOrDefaultAsync();
}`,questions:[
  sqr("¿Qué separa fundamentalmente el patrón CQRS?","Las operaciones que modifican estado (commands) de las que solo leen datos (queries), usando modelos distintos para cada una",["Las clases públicas de las privadas","El código síncrono del asíncrono","Los controladores de las vistas Razor"],0,"CQRS divide responsabilidades: un lado se encarga de comandos (escritura), el otro de consultas (lectura), cada uno con su propio modelo optimizado para su propósito."),
  sqr("En el ejemplo, CreateOrderHandler.Handle retorna solo 'order.Id' en vez de todo el objeto Order. ¿Por qué es coherente con CQRS?","Porque los commands se enfocan en modificar estado, no en devolver datos de lectura completos; para leer datos se usa una query separada",["Porque C# no permite retornar objetos completos desde un método async","Porque Order no tiene una propiedad Id","Porque los commands no pueden retornar ningún valor"],1,"Los commands en CQRS priorizan el efecto sobre el estado; devolver el modelo completo mezclaría responsabilidades de lectura dentro del lado de escritura."),
  sqr("¿Es obligatorio en CQRS usar dos bases de datos físicas distintas, una para comandos y otra para queries?","No, usar bases de datos separadas es una variante avanzada opcional; CQRS en su forma base solo separa los modelos de lectura y escritura, no necesariamente el almacenamiento físico",["Sí, sin dos bases de datos no es CQRS válido","Sí, y además deben estar en servidores físicos distintos","No, y de hecho CQRS prohíbe explícitamente usar más de una base de datos"],2,"Separar físicamente el almacenamiento de lectura y escritura es una extensión avanzada (frecuente junto a Event Sourcing), no un requisito para aplicar CQRS."),
  sqr("¿Qué característica debería tener siempre una query bien diseñada en CQRS?","Nunca debe modificar el estado del sistema, solo leer y proyectar datos",["Debe ejecutar siempre un INSERT antes de leer","Debe usar el mismo modelo que el command correspondiente","Debe retornar void en todos los casos"],3,"El principio de separación de responsabilidades exige que las queries sean de solo lectura (side-effect-free); cualquier efecto secundario de escritura pertenece al lado de comandos."),
  sqr("¿Qué ventaja aporta modelar GetOrderSummaryQuery con un DTO (OrderSummaryDto) en lugar de retornar la entidad Order completa del Dominio?","El modelo de lectura puede optimizarse para el caso de uso específico (proyección con solo los campos necesarios), sin acoplar la consulta a la forma completa de la entidad de Dominio",["Los DTOs son obligatorios por el compilador de C# al usar LINQ","Las entidades de Dominio no pueden usarse nunca en consultas LINQ","Los DTOs eliminan la necesidad de una base de datos"],0,"CQRS permite que el modelo de lectura (query side) se diseñe de forma independiente y optimizada, distinto del modelo rico de Dominio usado en el lado de escritura."),
  sqr("¿Qué trampa de entrevista es común sobre CQRS y Event Sourcing?","Son patrones complementarios pero independientes: se puede aplicar CQRS sin Event Sourcing, y viceversa",["CQRS y Event Sourcing son el mismo patrón con nombres distintos","Es imposible usar CQRS sin Event Sourcing","Event Sourcing es un requisito obligatorio de CQRS"],1,"Aunque suelen combinarse en sistemas complejos, CQRS (separar lectura/escritura) y Event Sourcing (persistir el estado como una secuencia de eventos) son conceptos distintos y pueden usarse por separado."),
  sqr("¿En qué escenario aporta más valor CQRS frente a un CRUD tradicional con un único modelo?","Cuando los requisitos de lectura y escritura son muy distintos en complejidad, volumen o forma de los datos (por ejemplo, escrituras transaccionales complejas y lecturas de reportes muy optimizadas)",["Siempre, en cualquier aplicación sin excepción, incluso un CRUD simple de una sola tabla","Nunca, CQRS solo sirve para aplicaciones sin base de datos","Solo cuando la aplicación no tiene usuarios concurrentes"],2,"CQRS agrega complejidad; su beneficio se nota cuando lectura y escritura tienen necesidades muy diferentes. En un CRUD simple, puede ser sobre-ingeniería."),
  sqr("¿Qué principio de diseño previo (visto en Clean Architecture) se relaciona con separar Command handlers de Query handlers en clases distintas?","Single Responsibility Principle (SRP): cada handler tiene una única razón de cambio, ya sea manejar un comando o resolver una consulta",["Liskov Substitution Principle, porque los handlers heredan entre sí","Interface Segregation, porque el DTO tiene pocos campos","Open/Closed, porque los handlers son sellados"],3,"Separar comandos y queries en handlers dedicados es una aplicación práctica de SRP: cada clase se especializa en una sola responsabilidad."),
  sqr("¿Qué representa 'CreateOrderCommand' como tipo 'record' en el ejemplo?","Un objeto inmutable que encapsula la intención de crear un pedido junto con los datos necesarios para hacerlo",["Una entidad de base de datos mapeada directamente por EF Core","Un controlador de ASP.NET Core","Una interfaz que otros commands deben implementar obligatoriamente"],0,"Los commands suelen modelarse como objetos de datos inmutables (records en C#) que representan la intención de realizar una acción, junto con los parámetros necesarios."),
  sqr("¿Qué NO es una consecuencia automática de aplicar CQRS?","Que la aplicación se vuelva automáticamente más rápida sin ningún trabajo adicional",["Que se puedan optimizar independientemente los modelos de lectura y escritura","Que las responsabilidades de comandos y consultas queden separadas en el código","Que sea más fácil razonar sobre qué operaciones modifican estado"],1,"CQRS es una herramienta de organización y separación de responsabilidades; el rendimiento depende de cómo se implementen y optimicen realmente los modelos, no es un beneficio automático."),
 ]},
 {id:"n06-mediatr",title:"MediatR",summary:"MediatR es una biblioteca .NET que implementa el patrón Mediator dentro del mismo proceso: desacopla quien envía una petición (por ejemplo, un controller) de quien la maneja (un handler), sin que se conozcan entre sí directamente. Se usa mucho junto con CQRS mediante IRequest<TResponse>/IRequestHandler<TRequest,TResponse> para commands/queries, e INotification/INotificationHandler para eventos de publicación múltiple. MediatR NO es un broker de mensajería externo ni una cola distribuida: toda la comunicación ocurre en memoria, dentro del mismo proceso.",code:`public record GetOrderQuery(int Id) : IRequest<OrderDto?>;

public class GetOrderHandler : IRequestHandler<GetOrderQuery, OrderDto?>
{
    private readonly AppDbContext _db;
    public GetOrderHandler(AppDbContext db) => _db = db;
    public Task<OrderDto?> Handle(GetOrderQuery request, CancellationToken ct) =>
        _db.Orders.Where(o => o.Id == request.Id)
                  .Select(o => new OrderDto(o.Id, o.Total)).FirstOrDefaultAsync(ct);
}

// En el controller:
public class OrdersController : ControllerBase
{
    private readonly IMediator _mediator;
    public OrdersController(IMediator mediator) => _mediator = mediator;

    [HttpGet("{id}")]
    public async Task<IActionResult> Get(int id)
    {
        var result = await _mediator.Send(new GetOrderQuery(id));
        return result is null ? NotFound() : Ok(result);
    }
}`,questions:[
  sqr("¿Qué es MediatR en el ecosistema .NET?","Una biblioteca que implementa el patrón Mediator dentro del mismo proceso, desacoplando quien envía una petición de quien la maneja",["Un broker de mensajería distribuido como RabbitMQ","Un servicio en la nube de Azure para colas","Una base de datos de eventos"],0,"MediatR opera completamente en memoria/en proceso; conecta un 'request' con su 'handler' registrado en el contenedor de DI, sin cruzar la red."),
  sqr("En el ejemplo, ¿qué interfaz implementa GetOrderQuery para poder enviarse mediante IMediator.Send?","IRequest<OrderDto?>",["INotification","IHostedService","IQueryable<OrderDto>"],1,"IRequest<TResponse> marca un tipo como un mensaje que espera una respuesta de tipo TResponse, manejado por un único IRequestHandler correspondiente."),
  sqr("¿Cuántos handlers pueden manejar un mismo IRequest<TResponse> enviado con mediator.Send(...)?","Exactamente uno, registrado para ese tipo de request",["Ninguno, Send no requiere handler","Todos los handlers registrados en la aplicación simultáneamente","Depende del hilo en que se ejecute"],2,"A diferencia de las notificaciones (INotification, que pueden tener múltiples handlers/suscriptores), un IRequest<TResponse> se resuelve con un único IRequestHandler."),
  sqr("¿Qué tipo de mensaje de MediatR permite que múltiples handlers reaccionen a un mismo evento (por ejemplo, 'OrderCreatedEvent')?","INotification, publicado con mediator.Publish(...)",["IRequest<TResponse>, enviado con mediator.Send(...)","IPipelineBehavior","IHostedService"],3,"INotification junto con mediator.Publish permite que varios INotificationHandler distintos reaccionen al mismo evento en memoria, similar a un patrón publish/subscribe local."),
  sqr("¿Qué trampa de entrevista es común sobre MediatR?","Confundirlo con un sistema de mensajería distribuido (como una cola o un broker); en realidad toda la comunicación de MediatR ocurre en memoria dentro del mismo proceso",["MediatR reemplaza completamente a ASP.NET Core","MediatR requiere una base de datos para funcionar","MediatR solo puede usarse en aplicaciones de consola"],0,"MediatR no envía mensajes por red ni persiste nada: es un mediador in-process que enruta un objeto request/notification a su handler registrado vía DI."),
  sqr("¿Qué provee un IPipelineBehavior<TRequest,TResponse> en MediatR?","Un punto de extensión para ejecutar lógica transversal (logging, validación, medición) antes y después de que el handler procese la petición",["Un mecanismo para enviar mensajes entre microservicios","Una forma de definir rutas HTTP","Un reemplazo directo de Entity Framework"],1,"Los pipeline behaviors envuelven la ejecución del handler, permitiendo cross-cutting concerns como validación (FluentValidation) o logging sin ensuciar cada handler."),
  sqr("En el controller del ejemplo, ¿qué gana OrdersController al depender de IMediator en vez de inyectar directamente GetOrderHandler?","El controller queda desacoplado de la implementación concreta del handler; solo conoce el mensaje (GetOrderQuery) que quiere enviar",["El controller deja de necesitar inyección de dependencias","El código se vuelve síncrono automáticamente","Elimina la necesidad de definir GetOrderQuery"],2,"Con MediatR, el emisor (controller) solo construye y envía el mensaje; no necesita conocer ni referenciar la clase concreta del handler que lo procesará."),
  sqr("¿Qué relación tiene MediatR con CQRS?","MediatR se usa frecuentemente como mecanismo de enrutamiento para separar y manejar commands y queries en handlers independientes, pero no es requisito de CQRS",["MediatR es la única forma válida de implementar CQRS","CQRS requiere obligatoriamente instalar el paquete MediatR","Son sinónimos exactos, MediatR es CQRS con otro nombre"],3,"CQRS es un patrón de diseño; MediatR es una herramienta que facilita implementarlo (un request/handler por cada command o query), pero se puede aplicar CQRS sin MediatR."),
  sqr("¿Qué sucede si se registra un IRequestHandler<GetOrderQuery, OrderDto?> más de una vez en el contenedor de DI para el mismo tipo de request?","MediatR no sabe determinísticamente cuál handler único usar para Send, lo cual normalmente produce un comportamiento incorrecto o una excepción en tiempo de ejecución",["MediatR ejecuta automáticamente ambos handlers en paralelo sin error","MediatR ignora los duplicados silenciosamente sin ningún efecto","El proyecto no compila"],0,"A diferencia de las notificaciones, un IRequest<TResponse> espera resolverse con un único handler; registrar múltiples implementaciones para el mismo request rompe esa expectativa."),
  sqr("¿Qué NO logra usar MediatR en una aplicación?","Comunicar procesos distintos a través de la red, como lo haría una cola de mensajes externa (RabbitMQ, Azure Service Bus)",["Desacoplar el emisor de una petición de su handler concreto","Aplicar lógica transversal mediante pipeline behaviors","Enrutar commands y queries a handlers específicos dentro del mismo proceso"],1,"MediatR resuelve el problema de desacoplamiento dentro de un mismo proceso .NET; para comunicación entre procesos o servicios distintos se necesita un mecanismo de mensajería real (cola, broker, bus)."),
 ]},
 {id:"n06-options-pattern",title:"Options pattern",summary:"El options pattern permite mapear secciones de configuración (appsettings.json, variables de entorno) a clases fuertemente tipadas usando services.Configure<T>(). Existen tres formas de consumirlo: IOptions<T> (singleton, se calcula una sola vez al inicio y no refleja cambios posteriores), IOptionsSnapshot<T> (scoped, se recalcula por cada scope/petición y sí refleja recargas de configuración) e IOptionsMonitor<T> (singleton, soporta notificaciones de cambio en vivo vía OnChange y puede inyectarse en servicios singleton).",code:`public class EmailSettings
{
    public string SmtpHost { get; set; } = "";
    public int Port { get; set; }
}

// Program.cs
builder.Services.Configure<EmailSettings>(builder.Configuration.GetSection("Email"));

// Consumo en un servicio singleton: necesita valores actualizables sin reiniciar
public class EmailSender
{
    private readonly IOptionsMonitor<EmailSettings> _options;
    public EmailSender(IOptionsMonitor<EmailSettings> options) => _options = options;
    public string CurrentHost => _options.CurrentValue.SmtpHost;
}`,questions:[
  sqr("¿Qué método registra el binding de una sección de configuración a una clase fuertemente tipada?","services.Configure<T>(configuration.GetSection(\"Seccion\"))",["services.AddSingleton<T>(new T())","services.Bind<T>()","services.MapOptions<T>()"],0,"Configure<T> registra el binding entre la sección de IConfiguration indicada y la clase T, habilitando su resolución vía IOptions/IOptionsSnapshot/IOptionsMonitor."),
  sqr("¿Cuál es la característica principal de IOptions<T>?","Es singleton: los valores se calculan una sola vez al iniciar la aplicación y no cambian aunque se recargue la configuración",["Se recalcula automáticamente en cada petición HTTP","Solo puede usarse dentro de controllers de MVC","Notifica cambios en vivo mediante un evento OnChange"],1,"IOptions<T> se resuelve como singleton con el valor capturado al arrancar; si el archivo de configuración cambia después, IOptions<T>.Value no lo refleja."),
  sqr("¿Qué tiempo de vida (lifetime) tiene IOptionsSnapshot<T> en el contenedor de DI?","Scoped: se recalcula una vez por cada scope (por ejemplo, por cada petición HTTP en ASP.NET Core)",["Singleton, igual que IOptions<T>","Transient, una instancia nueva en cada resolución dentro del mismo scope","No tiene lifetime, se resuelve de forma estática"],2,"IOptionsSnapshot<T> es scoped: refleja los valores de configuración vigentes al inicio de cada scope, siendo consistente dentro de esa misma petición."),
  sqr("¿Por qué IOptionsSnapshot<T> NO puede inyectarse directamente en un servicio registrado como singleton?","Porque un servicio singleton no puede depender de un servicio scoped sin capturarlo incorrectamente (violación de captive dependency); el contenedor de DI lo rechaza o genera un error en tiempo de ejecución",["Porque IOptionsSnapshot<T> no existe realmente en .NET","Porque los servicios singleton no pueden usar interfaces genéricas","Porque IOptionsSnapshot<T> solo funciona en Blazor"],3,"Inyectar un servicio scoped dentro de un singleton produciría una 'captive dependency': el singleton capturaría el valor del primer scope para siempre. ASP.NET Core valida y evita esto por defecto."),
  sqr("En el ejemplo, EmailSender es un servicio singleton y necesita reflejar cambios de configuración sin reiniciar la app. ¿Qué debería inyectar?","IOptionsMonitor<EmailSettings>, que es singleton y soporta valores actualizados en vivo",["IOptions<EmailSettings>, porque siempre refleja cambios","IOptionsSnapshot<EmailSettings>, porque es compatible con singletons","IConfiguration directamente sin ningún binding tipado"],0,"IOptionsMonitor<T> es singleton (compatible con servicios singleton) y expone CurrentValue actualizado junto con el evento OnChange para reaccionar a cambios de configuración."),
  sqr("¿Qué expone IOptionsMonitor<T> que no tienen IOptions<T> ni IOptionsSnapshot<T>?","Un evento OnChange(callback) para reaccionar en tiempo real cuando la configuración subyacente cambia",["Un método Value que nunca cambia","La capacidad de mapear configuración a una clase","Compatibilidad exclusiva con appsettings.json, sin variables de entorno"],1,"OnChange es exclusivo de IOptionsMonitor<T>: permite ejecutar código cada vez que se detecta un cambio en la fuente de configuración monitoreada."),
  sqr("¿Qué trampa de entrevista es común sobre IOptions<T> y la recarga de configuración?","Asumir que IOptions<T>.Value refleja automáticamente cambios en appsettings.json en caliente; en realidad IOptions<T> captura el valor una sola vez al inicio",["IOptions<T> siempre se actualiza automáticamente sin configuración adicional","IOptions<T> no puede usarse nunca en servicios singleton","IOptions<T> requiere Entity Framework para funcionar"],2,"Para configuración que cambia en caliente hay que usar IOptionsSnapshot<T> (por scope) o IOptionsMonitor<T> (con notificación); IOptions<T> queda fijo desde el arranque."),
  sqr("¿Qué interfaz permite validar las opciones al momento de construirlas, por ejemplo asegurando que Port esté en un rango válido?","IValidateOptions<T>, junto con .ValidateDataAnnotations() o .Validate(...) al registrar las opciones",["IOptionsFactory<T> exclusivamente, sin alternativas","ICacheOptions<T>","IConfigurationRoot"],3,"El options pattern soporta validación mediante ValidateDataAnnotations, Validate(Func<T,bool>) o implementando IValidateOptions<T>, fallando de forma temprana si la configuración es inválida."),
  sqr("¿Qué sucede si se llama a builder.Services.Configure<EmailSettings>(...) dos veces con distintas secciones para el mismo tipo EmailSettings, sin usar nombres de opciones (named options)?","La segunda configuración puede sobrescribir o combinarse con la primera de forma potencialmente confusa; para tener configuraciones distintas del mismo tipo se deben usar 'named options'",["Es imposible, el compilador lo rechaza","Se crean automáticamente dos servicios EmailSender distintos","Configure<T> ignora silenciosamente la segunda llamada sin ningún efecto"],0,"Cuando se necesita más de una configuración del mismo tipo (por ejemplo dos servidores SMTP distintos), .NET ofrece 'named options' con Configure<T>(name, ...) e IOptionsSnapshot<T>.Get(name)."),
  sqr("¿Cuál de estas afirmaciones sobre el options pattern es correcta?","Permite desacoplar el código de la fuente física de configuración (JSON, variables de entorno, Key Vault), exponiendo solo una clase C# fuertemente tipada",["Solo funciona si la configuración proviene de un archivo appsettings.json","Reemplaza por completo la necesidad de IConfiguration en toda la aplicación","Obliga a reiniciar la aplicación para leer cualquier valor de configuración"],1,"El options pattern es agnóstico de la fuente subyacente de IConfiguration; el código de negocio solo depende de la clase de opciones tipada, no de cómo se cargó el valor."),
 ]},
 {id:"n06-observability",title:"Observabilidad",summary:"La observabilidad se apoya en tres pilares: logs (eventos discretos con contexto), métricas (valores numéricos agregables en el tiempo, como contadores o histogramas) y trazas distribuidas (el recorrido de una petición a través de varios servicios). En .NET, ILogger<T> con logging estructurado, System.Diagnostics.Metrics para métricas y System.Diagnostics.Activity para trazas forman la base; OpenTelemetry es el estándar abierto para recolectar y exportar los tres pilares hacia backends como Prometheus, Grafana o Jaeger.",code:`public class OrderService
{
    private readonly ILogger<OrderService> _logger;
    public OrderService(ILogger<OrderService> logger) => _logger = logger;

    public async Task PlaceOrderAsync(Order order)
    {
        using var activity = MyActivitySource.StartActivity("PlaceOrder");
        activity?.SetTag("order.id", order.Id);

        _logger.LogInformation("Creando pedido {OrderId} para cliente {CustomerId}", order.Id, order.CustomerId);
        // ... lógica de negocio ...
    }
}`,questions:[
  sqr("¿Cuáles son los tres pilares clásicos de la observabilidad?","Logs, métricas y trazas (traces)",["Logs, backups y alertas","Métricas, tests unitarios y documentación","Trazas, contenedores y pipelines de CI/CD"],0,"Los tres pilares complementarios son: logs (eventos discretos con contexto), métricas (valores numéricos agregados en el tiempo) y trazas distribuidas (el camino de una petición entre servicios)."),
  sqr("En el código, ¿qué ventaja tiene escribir '_logger.LogInformation(\"Creando pedido {OrderId} para cliente {CustomerId}\", order.Id, order.CustomerId)' en vez de interpolar el string directamente con $\"...\"?","Genera logging estructurado: los valores quedan como campos independientes (OrderId, CustomerId) que un backend de logs puede indexar y filtrar, en vez de texto plano concatenado",["Es exactamente igual de eficiente y no aporta ninguna diferencia real","Evita que el log se escriba en la consola","Convierte automáticamente el log en una métrica"],1,"El logging estructurado con placeholders con nombre permite que los sistemas de logs (Seq, Elastic, Application Insights) traten cada valor como un campo consultable, no como texto plano.",`_logger.LogInformation("Creando pedido {OrderId} para cliente {CustomerId}", order.Id, order.CustomerId);
// mejor que:
_logger.LogInformation($"Creando pedido {order.Id} para cliente {order.CustomerId}");`),
  sqr("¿Qué representa un objeto Activity (System.Diagnostics.Activity) en el contexto de trazas distribuidas?","Un tramo (span) de una traza que representa una unidad de trabajo, con inicio, fin y posibles tags/atributos",["Un contador numérico que solo incrementa","Un registro de log individual sin contexto adicional","Una excepción capturada automáticamente"],2,"Un Activity representa un span dentro de una traza: mide duración y contexto de una operación, y puede anidarse con otros spans para reconstruir el recorrido completo de una petición."),
  sqr("¿Qué diferencia hay entre un log y una métrica?","Un log es un evento discreto con contexto detallado (útil para depurar un caso puntual); una métrica es un valor numérico agregable en el tiempo (útil para tendencias y alertas, como 'peticiones por segundo')",["Son exactamente lo mismo, solo cambia el nombre de la API","Las métricas siempre contienen texto libre; los logs siempre son numéricos","Un log solo puede existir en producción; una métrica solo en desarrollo"],3,"Los logs sirven para investigar eventos específicos; las métricas sirven para observar tendencias agregadas (throughput, latencia, tasa de error) de forma eficiente en almacenamiento."),
  sqr("¿Qué es OpenTelemetry?","Un estándar y conjunto de APIs/SDKs open source para instrumentar aplicaciones y exportar logs, métricas y trazas hacia backends de observabilidad",["Un proveedor específico de Microsoft exclusivo de Azure","Una base de datos de series temporales","Un reemplazo directo de ILogger<T> que elimina la necesidad de logging"],0,"OpenTelemetry define un estándar vendor-neutral para instrumentación; .NET tiene soporte nativo/paquetes oficiales para exportar hacia Prometheus, Jaeger, Application Insights, entre otros."),
  sqr("¿Qué ventaja aporta usar 'activity?.SetTag(\"order.id\", order.Id)' dentro de una traza distribuida?","Añade contexto (atributos) al span actual, permitiendo filtrar y correlacionar trazas por ese valor al analizarlas en un backend de observabilidad",["Convierte automáticamente la traza en un log estructurado","Detiene la ejecución hasta que se procese el tag","Elimina la necesidad de un ActivitySource"],1,"Los tags/atributos en un Activity enriquecen el span con datos de negocio (como el id del pedido), útiles para correlacionar y filtrar trazas en herramientas como Jaeger o Zipkin."),
  sqr("¿Qué trampa de entrevista es común sobre 'trazas distribuidas' en un sistema con múltiples microservicios?","Sin propagar un identificador de correlación (trace context) entre servicios, es imposible reconstruir el recorrido completo de una petición a través de todos ellos",["Las trazas distribuidas funcionan automáticamente sin ninguna configuración entre servicios distintos","Un solo log por servicio es suficiente para tener trazas distribuidas","Las trazas distribuidas solo aplican a aplicaciones monolíticas"],2,"El contexto de traza (trace id/span id) debe propagarse entre servicios (por ejemplo vía cabeceras HTTP W3C Trace Context) para poder correlacionar spans de distintos servicios en una sola traza."),
  sqr("¿Qué tipo de dato de System.Diagnostics.Metrics se usaría para contar cuántos pedidos se crearon en total, de forma monotónicamente creciente?","Un Counter<T>",["Un Histogram<T>","Un UpDownCounter<T> exclusivamente","Un Activity"],3,"Counter<T> representa un valor que solo incrementa (por ejemplo, total de pedidos creados); Histogram<T> se usa para distribuciones (como latencias), y UpDownCounter<T> para valores que suben y bajan."),
  sqr("¿Qué distingue a ILogger<T> de simplemente usar Console.WriteLine para diagnosticar una aplicación en producción?","ILogger<T> integra niveles de severidad (LogLevel), logging estructurado, y puede enviar los eventos a múltiples proveedores/backends configurables sin cambiar el código de negocio",["Console.WriteLine es más rápido en absolutamente todos los escenarios de producción","ILogger<T> no puede escribir en la consola","Son intercambiables sin ninguna diferencia práctica"],0,"ILogger<T> desacopla el código de negocio del destino de los logs (consola, archivo, Application Insights, Seq, etc.) y soporta niveles (Debug, Information, Warning, Error) y logging estructurado."),
  sqr("¿Por qué la observabilidad es especialmente importante en arquitecturas con background services o mensajería asíncrona (vistas en el siguiente módulo)?","Porque no hay una petición HTTP síncrona que un desarrollador pueda seguir paso a paso con un debugger; logs, métricas y trazas son la forma principal de entender qué ocurrió en procesos en segundo plano",["Porque los background services no generan ningún log por defecto y no se puede instrumentar nada","Porque la observabilidad solo aplica a aplicaciones web con UI","Porque los background services siempre corren en un proceso distinto sin ninguna forma de correlacionarlos"],1,"En procesos asíncronos y en segundo plano, la observabilidad (logs estructurados, métricas de cola, trazas distribuidas) es la herramienta principal para diagnosticar problemas, ya que no hay una sesión de depuración interactiva en producción."),
 ]},
 {id:"n06-challenge",title:"Reto de arquitectura",summary:"Reto integrador del módulo: combina Clean Architecture, SOLID, Repository/Unit of Work, CQRS, MediatR, options pattern y observabilidad en un mismo flujo, para reconocer cómo estas piezas encajan al diseñar un servicio de aplicación real.",code:`// Command (CQRS) manejado por MediatR, que respeta Clean Architecture y DIP
public record CreateOrderCommand(int CustomerId, List<OrderLine> Lines) : IRequest<int>;

public class CreateOrderHandler : IRequestHandler<CreateOrderCommand, int>
{
    private readonly IUnitOfWork _uow;       // abstracción del Dominio (DIP)
    private readonly ILogger<CreateOrderHandler> _logger; // observabilidad

    public CreateOrderHandler(IUnitOfWork uow, ILogger<CreateOrderHandler> logger)
    { _uow = uow; _logger = logger; }

    public async Task<int> Handle(CreateOrderCommand request, CancellationToken ct)
    {
        var order = new Order(request.CustomerId, request.Lines);
        await _uow.Orders.AddAsync(order);
        await _uow.CompleteAsync(); // Unit of Work: una sola transacción
        _logger.LogInformation("Pedido {OrderId} creado", order.Id);
        return order.Id;
    }
}`,questions:[
  sqr("En el handler del reto, ¿qué principio explica que CreateOrderHandler dependa de IUnitOfWork y no de una clase EfUnitOfWork concreta?","DIP (Dependency Inversion Principle), la 'D' de SOLID, alineado con la regla de dependencia de Clean Architecture",["OCP, porque el handler no puede extenderse","LSP, porque IUnitOfWork no tiene subtipos","ISP, porque IUnitOfWork es una interfaz pequeña"],0,"El handler (capa de Aplicación) depende de la abstracción IUnitOfWork definida en capas internas; la implementación concreta con EF Core vive en Infraestructura, respetando DIP y el dependency rule."),
  sqr("¿Por qué CreateOrderCommand se define como 'record' que implementa IRequest<int> en vez de simplemente un método público en un controller?","Porque desacopla la intención de crear un pedido (el mensaje) de quién la maneja, permitiendo que MediatR enrute la petición al handler correspondiente sin que el controller conozca la implementación",["Porque los controllers de ASP.NET Core no pueden tener métodos públicos","Porque record es la única forma de compilar un IRequest<T>","Porque evita que el pedido se pueda validar"],1,"Modelar la operación como un mensaje (IRequest<int>) enviado vía MediatR es la aplicación práctica de CQRS junto con el patrón Mediator, separando el 'qué se quiere hacer' de 'quién lo procesa'."),
  sqr("¿Qué patrón garantiza que 'await _uow.Orders.AddAsync(order)' y 'await _uow.CompleteAsync()' se confirmen como una sola operación atómica?","Unit of Work",["Repository, por sí solo, sin Unit of Work","CQRS","Options pattern"],2,"Unit of Work agrupa las operaciones pendientes de sus repositorios y las confirma juntas con CompleteAsync (que internamente llama a SaveChangesAsync), garantizando atomicidad."),
  sqr("Si CreateOrderHandler retornara la entidad Order completa en lugar de solo 'order.Id', ¿qué principio de CQRS estaría comprometiendo?","La separación entre el lado de comandos (que debería enfocarse en el efecto, no en exponer el modelo completo de lectura) y el lado de queries",["SRP, porque el método tendría dos responsabilidades de compilación","El dependency rule de Clean Architecture","El principio de Repository, porque Order no es una interfaz"],3,"CQRS sugiere que los commands no dupliquen la responsabilidad de las queries devolviendo modelos de lectura completos; devolver solo el id (o un resultado mínimo) mantiene esa separación."),
  sqr("¿Qué rol cumple ILogger<CreateOrderHandler> inyectado en el handler, en términos de observabilidad?","Permite emitir logs estructurados (con el id del pedido como campo) que ayudan a diagnosticar y monitorear el comportamiento del sistema en producción",["Valida los datos del pedido antes de guardarlo","Reemplaza la necesidad de IUnitOfWork","Convierte automáticamente el handler en un background service"],0,"ILogger<T> es la herramienta de logging estructurado de .NET; en este handler documenta el evento de negocio 'pedido creado' con contexto (OrderId) útil para observabilidad."),
  sqr("Si el equipo quisiera parametrizar por configuración un límite máximo de líneas por pedido (MaxOrderLines), ¿qué mecanismo del módulo sería el más adecuado?","El options pattern, inyectando IOptions<T> o IOptionsSnapshot<T> con una clase OrderSettings que exponga MaxOrderLines",["Hardcodear el valor directamente en el handler sin posibilidad de cambiarlo","Guardar el límite como un campo estático mutable en Order","Crear una nueva base de datos exclusiva para ese valor"],1,"El options pattern está diseñado exactamente para este caso: exponer valores configurables (como límites de negocio) como una clase fuertemente tipada, sin hardcodearlos."),
  sqr("¿Qué capa del proyecto, siguiendo Clean Architecture, debería contener la definición de la interfaz IUnitOfWork usada por CreateOrderHandler?","Una capa interna (Dominio o Aplicación), para que Infraestructura la implemente cumpliendo el dependency rule",["El proyecto de Infraestructura, ya que ahí se usa EF Core","El proyecto de Presentación (la Web API)","No importa el proyecto, siempre que compile"],0,"La interfaz debe vivir en una capa interna (a menudo Aplicación o Dominio) para que las capas externas (Infraestructura) dependan de ella al implementarla, y no al revés."),
  sqr("¿Qué principio SOLID se refuerza si, en vez de una interfaz IUnitOfWork gigante con métodos de todas las entidades del sistema, se define con solo las propiedades de repositorio realmente necesarias (Orders, Customers)?","ISP (Interface Segregation Principle)",["LSP, porque no hay herencia de clases involucrada","OCP, porque la interfaz no puede modificarse nunca","DIP, porque IUnitOfWork ya es una abstracción por definición"],1,"Mantener la interfaz enfocada solo en lo que los consumidores realmente necesitan, en vez de una interfaz 'gorda' con todos los repositorios del sistema, es una aplicación directa de ISP."),
  sqr("¿Qué diferencia hay entre lo que hace MediatR y lo que haría, en el módulo siguiente, una cola de mensajes como las usadas por un background service?","MediatR enruta el mensaje CreateOrderCommand a su handler dentro del mismo proceso y de forma síncrona respecto al llamador; una cola de mensajes desacopla al emisor del consumidor entre procesos, de forma asíncrona y potencialmente distribuida",["Son exactamente equivalentes y pueden sustituirse sin ningún cambio de comportamiento","MediatR siempre persiste los mensajes en disco igual que una cola","Una cola de mensajes solo puede usarse dentro del mismo proceso, igual que MediatR"],2,"MediatR resuelve el desacoplamiento intra-proceso; una cola de mensajes (tema del módulo de mensajería) resuelve el desacoplamiento entre procesos/servicios, con entrega asíncrona y normalmente persistida."),
  sqr("¿Qué combinación de conceptos del módulo se refleja mejor en el flujo completo del reto (Command con MediatR + IUnitOfWork inyectado por interfaz + logging estructurado)?","Clean Architecture (dependencias hacia adentro) + SOLID (DIP) + CQRS (separación de comandos) + Unit of Work (transacción atómica) + observabilidad (logs estructurados), todos colaborando en un mismo caso de uso",["Solo CQRS, el resto de conceptos no participan en este código","Solo el options pattern, ya que todo depende de configuración","Ninguno de los conceptos del módulo, es código genérico sin relación con ellos"],3,"El handler del reto es un ejemplo condensado de cómo los patrones del módulo se combinan en la práctica: arquitectura limpia, principios SOLID, separación CQRS, transacciones con Unit of Work y observabilidad con logging estructurado."),
 ]},
 {id:"n07-ihostedservice",title:"IHostedService",summary:"IHostedService es la interfaz base del Generic Host de .NET para ejecutar código de fondo dentro del mismo proceso de la aplicación: define StartAsync(CancellationToken) y StopAsync(CancellationToken). Se registra con services.AddHostedService<T>() y el host lo administra automáticamente, invocando StartAsync al arrancar la aplicación y StopAsync durante un apagado ordenado (graceful shutdown). StartAsync debe retornar rápido: si contiene trabajo de larga duración sin ceder el control, retrasa el arranque completo de la aplicación.",code:`public class CacheWarmupService : IHostedService
{
    private readonly IMemoryCache _cache;
    public CacheWarmupService(IMemoryCache cache) => _cache = cache;

    public Task StartAsync(CancellationToken cancellationToken)
    {
        _cache.Set("config:loaded", true);
        return Task.CompletedTask; // retorna rápido, no bloquea el arranque
    }

    public Task StopAsync(CancellationToken cancellationToken) => Task.CompletedTask;
}

// Program.cs
builder.Services.AddHostedService<CacheWarmupService>();`,questions:[
  sqr("¿Qué dos métodos define la interfaz IHostedService?","StartAsync(CancellationToken) y StopAsync(CancellationToken)",["Run() y Stop()","ExecuteAsync() y DisposeAsync()","OnStart() y OnStop() sin parámetros"],0,"IHostedService define exactamente StartAsync y StopAsync; el Generic Host los invoca automáticamente en el arranque y apagado de la aplicación."),
  sqr("¿Qué método registra un IHostedService en el contenedor de DI para que el host lo administre?","services.AddHostedService<T>()",["services.AddSingleton<T>() únicamente, sin nada más","services.AddBackgroundWorker<T>()","services.RunHosted<T>()"],1,"AddHostedService<T>() registra el servicio como singleton y además lo agrega a la colección de servicios hospedados que el host inicia/detiene automáticamente."),
  sqr("En el ejemplo, ¿por qué StartAsync retorna Task.CompletedTask en vez de ejecutar trabajo de larga duración de forma síncrona bloqueante?","Porque StartAsync debe completarse rápidamente; el host espera a que termine antes de continuar con el arranque de la aplicación, y bloquear ahí retrasaría todo el inicio",["Porque Task.CompletedTask es obligatorio por la firma del método","Porque StartAsync no puede contener código asíncrono","Porque CacheWarmupService no implementa correctamente IHostedService"],2,"El host invoca y espera StartAsync de cada servicio hospedado antes de terminar de arrancar; trabajo largo y continuo debe delegarse (por ejemplo a un BackgroundService con ExecuteAsync) en lugar de bloquear StartAsync."),
  sqr("¿Cuándo invoca el Generic Host el método StopAsync de un IHostedService?","Durante un apagado ordenado (graceful shutdown) de la aplicación, por ejemplo al recibir una señal de terminación",["Cada vez que se recibe una petición HTTP","Solo si ocurre una excepción no controlada","Nunca automáticamente; hay que llamarlo manualmente"],3,"StopAsync forma parte del ciclo de vida gestionado por el host: se invoca al apagar la aplicación de forma ordenada, permitiendo liberar recursos o completar trabajo pendiente."),
  sqr("¿Pueden registrarse múltiples IHostedService en la misma aplicación?","Sí, y el host inicia todos sus StartAsync (en el orden de registro) y detiene todos sus StopAsync durante el apagado",["No, solo se permite un IHostedService por aplicación","Sí, pero solo si están en ensamblados distintos","No, a menos que se use un contenedor de DI de terceros"],0,"El host mantiene una colección de todos los IHostedService registrados y coordina su arranque y apagado; es común tener varios (por ejemplo, un warmup y un worker de colas) en la misma aplicación."),
  sqr("¿Qué diferencia clave hay entre IHostedService y un método normal llamado manualmente en Program.cs antes de app.Run()?","IHostedService se integra al ciclo de vida gestionado por el host, incluyendo el apagado ordenado con StopAsync y CancellationToken; una llamada manual no participa en ese ciclo de vida ni recibe la señal de shutdown",["No hay ninguna diferencia real entre ambos enfoques","Un método manual siempre es más rápido que IHostedService","IHostedService solo funciona en aplicaciones de consola, no en Web API"],1,"El valor de IHostedService es integrarse al pipeline del host: recibe notificación de apagado vía StopAsync/CancellationToken, algo que una simple llamada de inicialización no ofrece."),
  sqr("¿Qué trampa de entrevista es común sobre StartAsync en IHostedService?","Creer que es el lugar ideal para ejecutar trabajo continuo de fondo (como un bucle infinito) sin retornar; en realidad StartAsync debe iniciar el trabajo y retornar pronto, dejando el trabajo continuo a otra construcción como BackgroundService",["StartAsync siempre se ejecuta en un hilo separado automáticamente sin bloquear nada","StartAsync no puede ser async Task, solo void","StartAsync se ejecuta una vez por cada petición HTTP"],2,"Colocar un bucle infinito directamente en StartAsync sin ceder el control bloquearía el arranque de la aplicación indefinidamente; para trabajo continuo se usa la clase base BackgroundService."),
  sqr("¿Qué parámetro recibe StartAsync que permite cancelar el arranque si el host lo solicita (por ejemplo, por timeout de inicio)?","Un CancellationToken",["Un objeto HttpContext","Un IServiceProvider adicional","Ninguno, StartAsync no recibe parámetros"],3,"StartAsync(CancellationToken cancellationToken) recibe un token que se cancela si el proceso de arranque del host se interrumpe o excede el tiempo permitido."),
  sqr("¿Qué implementa realmente BackgroundService (una clase abstracta que se verá en el siguiente paso) por debajo?","BackgroundService implementa IHostedService y provee una plantilla con el método abstracto ExecuteAsync para simplificar el trabajo continuo en segundo plano",["BackgroundService no tiene relación con IHostedService","BackgroundService reemplaza completamente al Generic Host","BackgroundService es una interfaz, no una clase"],0,"BackgroundService es una clase base abstracta que implementa IHostedService y maneja correctamente el patrón de iniciar una tarea continua sin bloquear StartAsync."),
  sqr("Si CacheWarmupService.StartAsync lanza una excepción no controlada, ¿qué ocurre normalmente con el arranque de la aplicación host?","Por defecto, el host detiene el arranque de la aplicación, ya que una excepción no controlada en StartAsync de un servicio hospedado se considera un fallo crítico de inicio",["El host la ignora silenciosamente y continúa arrancando el resto de servicios","Solo se registra en el log sin ningún otro efecto en el arranque","La excepción se convierte automáticamente en una respuesta HTTP 500"],1,"Una excepción no controlada durante StartAsync de un IHostedService generalmente hace que el host falle al iniciar la aplicación, ya que se considera parte crítica del proceso de arranque."),
 ]},
 {id:"n07-backgroundservice",title:"BackgroundService",summary:"BackgroundService es una clase base abstracta de Microsoft.Extensions.Hosting que implementa IHostedService y simplifica el patrón de trabajo continuo en segundo plano: solo hay que sobrescribir ExecuteAsync(CancellationToken). Internamente, su StartAsync invoca ExecuteAsync y NO espera (await) a que termine, sino que guarda la Task resultante y retorna de inmediato, evitando bloquear el arranque del host. Su StopAsync señala el CancellationToken y espera (con un timeout) a que la tarea en curso termine de forma ordenada.",code:`public class QueueProcessorWorker : BackgroundService
{
    private readonly ILogger<QueueProcessorWorker> _logger;
    public QueueProcessorWorker(ILogger<QueueProcessorWorker> logger) => _logger = logger;

    protected override async Task ExecuteAsync(CancellationToken stoppingToken)
    {
        while (!stoppingToken.IsCancellationRequested)
        {
            _logger.LogInformation("Procesando cola en {Time}", DateTimeOffset.UtcNow);
            await Task.Delay(TimeSpan.FromSeconds(5), stoppingToken);
        }
    }
}

// Program.cs
builder.Services.AddHostedService<QueueProcessorWorker>();`,questions:[
  sqr("¿Qué método abstracto se debe sobrescribir al heredar de BackgroundService?","ExecuteAsync(CancellationToken stoppingToken)",["StartAsync(CancellationToken)","RunAsync() sin parámetros","Main(CancellationToken)"],0,"BackgroundService expone ExecuteAsync como el único método que las clases derivadas deben implementar para definir el trabajo continuo de fondo."),
  sqr("¿Qué hace internamente el StartAsync de BackgroundService (heredado de IHostedService) con la Task que retorna ExecuteAsync?","La invoca y guarda la Task resultante SIN esperarla (sin await), retornando de inmediato para no bloquear el arranque del host",["La espera (await) por completo antes de retornar, bloqueando el arranque hasta que ExecuteAsync termine","La ejecuta en un nuevo proceso del sistema operativo","La descarta inmediatamente sin ejecutar ExecuteAsync"],1,"Si StartAsync esperara la Task completa de ExecuteAsync (que normalmente contiene un bucle continuo), el host nunca terminaría de arrancar; por eso BackgroundService no hace await de esa tarea en StartAsync."),
  sqr("En el ejemplo, ¿por qué el bucle usa 'while (!stoppingToken.IsCancellationRequested)' en vez de 'while (true)'?","Para que el worker termine su bucle de forma ordenada cuando el host solicita el apagado, en vez de seguir ejecutándose indefinidamente ignorando la señal de cancelación",["Porque 'while (true)' no compila dentro de un método async","Porque stoppingToken siempre está cancelado desde el inicio","Porque IsCancellationRequested cambia el valor de retorno del método"],2,"Revisar stoppingToken.IsCancellationRequested permite que el ciclo salga de forma cooperativa cuando el host señala el apagado, en vez de que StopAsync tenga que esperar forzadamente o hacer timeout."),
  sqr("¿Qué hace el 'await Task.Delay(TimeSpan.FromSeconds(5), stoppingToken)' del ejemplo si se solicita el apagado del host durante esos 5 segundos de espera?","Task.Delay lanza una OperationCanceledException/TaskCanceledException al cancelarse el token, interrumpiendo la espera antes de completar los 5 segundos",["Ignora el token y completa siempre los 5 segundos completos","Provoca un deadlock del hilo actual","Reinicia el contador de 5 segundos desde cero"],3,"Pasar el CancellationToken a Task.Delay permite que la espera se cancele inmediatamente cuando se solicita el apagado, en vez de bloquear el cierre ordenado del worker hasta que expire el delay."),
  sqr("¿Qué hace StopAsync de BackgroundService cuando el host solicita el apagado?","Señala (cancela) el CancellationToken que recibe ExecuteAsync y espera a que la tarea en curso termine, dentro de un tiempo límite configurable",["Mata el hilo del worker inmediatamente sin darle oportunidad de terminar limpio","No hace nada, StopAsync está vacío en BackgroundService","Reinicia ExecuteAsync desde el principio"],0,"BackgroundService.StopAsync cancela el token que ExecuteAsync recibe como stoppingToken y espera (await) la tarea en ejecución, permitiendo un cierre cooperativo antes de forzar la finalización."),
  sqr("¿Es correcto decir que BackgroundService reemplaza a una cola de mensajes externa (como RabbitMQ)?","No: BackgroundService ejecuta trabajo en segundo plano dentro del mismo proceso de la aplicación; no es un sistema de mensajería externo ni sobrevive a un reinicio del proceso",["Sí, BackgroundService es funcionalmente idéntico a una cola de mensajes distribuida","Sí, porque ambos usan CancellationToken internamente","No hay relación entre ambos conceptos en absoluto, ni siquiera pueden combinarse"],1,"BackgroundService es un mecanismo in-process para trabajo continuo; suele combinarse con una cola (en memoria o externa) como fuente de trabajo, pero no sustituye a un broker de mensajería real."),
  sqr("¿Qué ocurre si ExecuteAsync lanza una excepción no controlada mientras el worker está corriendo?","Por defecto, la Task de fondo queda en estado fallado (faulted); dependiendo de la configuración de HostOptions, esto puede provocar que el host completo se detenga",["El host ignora la excepción y reinicia automáticamente ExecuteAsync sin ninguna configuración adicional","La excepción se convierte automáticamente en una respuesta HTTP para el cliente","No pasa nada, el worker sigue ejecutándose desde el punto exacto donde falló"],3,"Una excepción no controlada en ExecuteAsync deja la tarea en estado 'Faulted'; sin manejo explícito (try/catch, o configurando BackgroundServiceExceptionBehavior), puede provocar que la aplicación entera se detenga."),
  sqr("¿Qué trampa de entrevista es común sobre el uso de Task.Run dentro de ExecuteAsync de un BackgroundService?","Envolver innecesariamente el trabajo con Task.Run cuando ExecuteAsync ya se ejecuta en un contexto sin bloquear el hilo que lo invocó agrega una indirección/hilo del thread pool sin beneficio real",["Task.Run es obligatorio dentro de ExecuteAsync, de lo contrario el worker no arranca","Task.Run convierte automáticamente el código en más eficiente en cualquier escenario","ExecuteAsync no puede contener llamadas asíncronas reales"],0,"Como el StartAsync de BackgroundService ya no bloquea el hilo que lo invoca (no hace await de la tarea completa), envolver el cuerpo de ExecuteAsync en Task.Run suele ser una indirección innecesaria."),
  sqr("¿Cuál es la relación exacta entre BackgroundService e IHostedService?","BackgroundService es una clase abstracta que implementa IHostedService, proporcionando una plantilla lista para trabajo continuo mediante ExecuteAsync",["Son interfaces hermanas sin relación de herencia","IHostedService hereda de BackgroundService","Son mutuamente excluyentes: no se puede usar BackgroundService si ya se implementa IHostedService"],1,"BackgroundService implementa IHostedService internamente y añade la lógica estándar de arrancar ExecuteAsync sin bloquear, y de cancelar/esperar esa tarea al detenerse."),
  sqr("¿Qué característica de QueueProcessorWorker en el ejemplo confirma que sigue corriendo dentro del mismo proceso de la aplicación .NET (no en un proceso externo)?","Comparte el mismo contenedor de DI y se registra junto a los demás servicios de la aplicación con AddHostedService<T>(), ejecutándose en el mismo proceso .NET que el resto de la app",["Usa una dirección IP y puerto configurados explícitamente","Requiere una instalación separada de un servicio de Windows","Necesita un contenedor Docker adicional obligatoriamente"],2,"El hecho de registrarse en el mismo IServiceCollection y ser administrado por el mismo Generic Host confirma que corre in-process junto con el resto de la aplicación."),
 ]},
 {id:"n07-queues",title:"Queues",summary:"Una cola desacopla productores de consumidores: los productores encolan trabajo y los consumidores lo procesan a su propio ritmo, sin bloquear al productor. En .NET, System.Threading.Channels ofrece una cola en memoria y thread-safe (Channel<T>) ideal para comunicar un BackgroundService con el resto de la aplicación dentro del mismo proceso. Para escenarios que cruzan procesos, servidores o necesitan persistencia y garantías de entrega, se usan colas externas como RabbitMQ, Azure Service Bus o Kafka, que Channel<T> NO reemplaza.",code:`public interface IOrderQueue
{
    ValueTask EnqueueAsync(Order order, CancellationToken ct);
    IAsyncEnumerable<Order> ReadAllAsync(CancellationToken ct);
}

public class ChannelOrderQueue : IOrderQueue
{
    private readonly Channel<Order> _channel = Channel.CreateUnbounded<Order>();
    public ValueTask EnqueueAsync(Order order, CancellationToken ct) => _channel.Writer.WriteAsync(order, ct);
    public IAsyncEnumerable<Order> ReadAllAsync(CancellationToken ct) => _channel.Reader.ReadAllAsync(ct);
}

public class OrderQueueWorker : BackgroundService
{
    private readonly IOrderQueue _queue;
    public OrderQueueWorker(IOrderQueue queue) => _queue = queue;
    protected override async Task ExecuteAsync(CancellationToken stoppingToken)
    {
        await foreach (var order in _queue.ReadAllAsync(stoppingToken))
        {
            // procesar order
        }
    }
}`,questions:[
  sqr("¿Qué problema resuelve encolar trabajo (por ejemplo, un pedido) en vez de procesarlo directamente dentro de la petición HTTP que lo originó?","Desacopla al productor (el endpoint HTTP) del consumidor (el worker), permitiendo responder rápido al cliente mientras el procesamiento ocurre de forma asíncrona en segundo plano",["Elimina por completo la necesidad de manejar excepciones","Hace que el procesamiento sea siempre instantáneo","Evita tener que usar async/await en el endpoint"],0,"Encolar permite que el endpoint retorne rápido (por ejemplo, un 202 Accepted) mientras el trabajo pesado se procesa de forma independiente en un worker, mejorando la latencia percibida por el cliente."),
  sqr("¿Qué tipo de la biblioteca estándar de .NET provee una cola en memoria thread-safe pensada para comunicación productor/consumidor dentro del mismo proceso?","Channel<T> de System.Threading.Channels",["List<T> con lock manual como única alternativa","ConcurrentBag<T> exclusivamente","DataTable"],1,"Channel<T> está diseñado específicamente para el patrón productor/consumidor asíncrono, con soporte nativo para IAsyncEnumerable y backpressure mediante colas acotadas (bounded)."),
  sqr("En el ejemplo, ¿qué hace 'Channel.CreateUnbounded<Order>()' respecto al tamaño de la cola?","Crea una cola sin límite de capacidad: el escritor nunca se bloquea por falta de espacio, pero el uso de memoria puede crecer sin control si el consumidor no da abasto",["Limita la cola a exactamente un elemento a la vez","Crea automáticamente varios consumidores en paralelo","Persiste los elementos en disco para sobrevivir a un reinicio"],2,"Un canal 'unbounded' no aplica backpressure por capacidad; si el productor es mucho más rápido que el consumidor, la cola puede crecer indefinidamente y consumir memoria."),
  sqr("¿Qué alternativa a CreateUnbounded aplicaría backpressure, haciendo que el productor espere si la cola está llena?","Channel.CreateBounded<T>(capacity), que bloquea (de forma asíncrona) al escritor cuando se alcanza la capacidad máxima configurada",["Channel.CreateUnbounded<T>() con un parámetro adicional de timeout","No existe tal mecanismo en Channel<T>","Usar una List<T> con un límite manual de elementos"],3,"CreateBounded<T> limita la cantidad de elementos en el canal; cuando se llena, WriteAsync espera (de forma no bloqueante para el hilo) hasta que haya espacio, aplicando backpressure real al productor."),
  sqr("¿Qué ocurre con los elementos encolados en un Channel<T> en memoria si el proceso de la aplicación se reinicia o se cae inesperadamente?","Se pierden por completo, ya que Channel<T> no persiste su contenido en ningún almacenamiento durable",["Se recuperan automáticamente al reiniciar el proceso","Se replican automáticamente a otro servidor","Quedan guardados en la base de datos configurada por defecto"],0,"Channel<T> es puramente en memoria; a diferencia de una cola externa persistente (como Azure Service Bus), no sobrevive a un reinicio ni ofrece garantías de entrega tras una caída."),
  sqr("¿Cuándo conviene usar una cola externa (RabbitMQ, Azure Service Bus, Kafka) en lugar de Channel<T>?","Cuando el productor y el consumidor están en procesos o servidores distintos, o se necesita persistencia/garantías de entrega ante caídas del proceso",["Siempre, Channel<T> nunca debería usarse en producción","Nunca, Channel<T> es siempre preferible por ser más simple","Solo cuando la aplicación no usa async/await"],1,"Channel<T> resuelve comunicación in-process; cuando se necesita cruzar procesos/servidores, o garantizar que un mensaje no se pierda aunque el proceso se caiga, se requiere una cola externa real."),
  sqr("¿Qué patrón implementa OrderQueueWorker al hacer 'await foreach (var order in _queue.ReadAllAsync(stoppingToken))' dentro de ExecuteAsync?","Un consumidor asíncrono que procesa elementos a medida que llegan a la cola, deteniéndose de forma ordenada cuando se cancela stoppingToken",["Un patrón de sondeo (polling) con Thread.Sleep en un bucle síncrono","Una consulta LINQ diferida sin ningún efecto en segundo plano","Un patrón de reintentos exponenciales"],2,"ReadAllAsync retorna un IAsyncEnumerable<T> que 'await foreach' consume de forma asíncrona y eficiente, terminando limpiamente cuando el token de cancelación se activa."),
  sqr("¿Qué trampa de entrevista es común sobre 'usar una cola' en el contexto de background services?","Asumir que cualquier cola (incluyendo una en memoria como Channel<T>) ofrece las mismas garantías de persistencia y tolerancia a fallos que una cola de mensajería distribuida real",["Las colas siempre requieren una base de datos relacional por debajo","Una cola en memoria es siempre más lenta que una externa en cualquier escenario","Channel<T> solo puede tener un único consumidor posible en su API"],0,"Es un error común pensar que 'encolar' automáticamente da garantías de durabilidad; una cola en memoria como Channel<T> pierde su contenido si el proceso muere, a diferencia de un broker externo bien configurado."),
  sqr("¿Qué interfaz define ChannelOrderQueue en el ejemplo para exponer un método de escritura y otro de lectura de la cola?","IOrderQueue, con EnqueueAsync y ReadAllAsync",["IHostedService, para ser administrada por el host directamente","IDisposable únicamente","No define ninguna interfaz, se usa Channel<Order> directamente en cada consumidor"],1,"El ejemplo abstrae Channel<Order> detrás de IOrderQueue, permitiendo inyectar la cola en otros servicios sin acoplarlos directamente al tipo Channel<T>."),
  sqr("¿Puede haber múltiples consumidores leyendo del mismo Channel<T> simultáneamente?","Sí, Channel<T> soporta múltiples lectores y escritores concurrentes de forma segura (thread-safe), repartiéndose los elementos entre los consumidores activos",["No, un Channel<T> solo admite un lector durante toda su vida","Solo si se crea con CreateUnbounded y nunca con CreateBounded","No, requeriría envolver manualmente cada operación con lock"],0,"Channel<T> está diseñado para ser thread-safe con múltiples productores y/o consumidores; varios workers pueden leer del mismo Reader y cada elemento se entrega a un solo consumidor."),
 ]},
 {id:"n07-health-checks",title:"Health checks",summary:"Los health checks exponen el estado de una aplicación y sus dependencias (base de datos, cola, servicio externo) a través de un endpoint HTTP, típicamente usado por orquestadores (Kubernetes) o balanceadores de carga para decidir si enrutar tráfico o reiniciar una instancia. En ASP.NET Core se implementan con IHealthCheck.CheckHealthAsync, que retorna un HealthCheckResult (Healthy, Degraded o Unhealthy), y se registran con AddHealthChecks()/AddCheck<T>() y se exponen con MapHealthChecks(\"/health\").",code:`public class QueueHealthCheck : IHealthCheck
{
    private readonly IOrderQueue _queue;
    public QueueHealthCheck(IOrderQueue queue) => _queue = queue;

    public Task<HealthCheckResult> CheckHealthAsync(HealthCheckContext context, CancellationToken cancellationToken = default)
    {
        var isHealthy = _queue != null;
        return Task.FromResult(isHealthy
            ? HealthCheckResult.Healthy("La cola está disponible")
            : HealthCheckResult.Unhealthy("La cola no está disponible"));
    }
}

// Program.cs
builder.Services.AddHealthChecks()
    .AddCheck<QueueHealthCheck>("queue")
    .AddDbContextCheck<AppDbContext>("database");

app.MapHealthChecks("/health");`,questions:[
  sqr("¿Qué interfaz debe implementar una clase para definir un health check personalizado?","IHealthCheck, con el método CheckHealthAsync",["IHostedService","IHealthMonitor","IDiagnosticsCheck"],0,"IHealthCheck define CheckHealthAsync(HealthCheckContext, CancellationToken), que retorna un HealthCheckResult indicando el estado de la dependencia evaluada."),
  sqr("¿Cuáles son los tres posibles estados que puede retornar un HealthCheckResult?","Healthy, Degraded y Unhealthy",["Ok, Warning y Error","Up, Down y Unknown","Green, Yellow y Red exclusivamente como enum"],1,"HealthCheckResult expone los estados Healthy (todo bien), Degraded (funciona pero con problemas) y Unhealthy (no funcional), usados por HealthStatus."),
  sqr("¿Qué método de configuración registra un health check personalizado y le asigna un nombre?","services.AddHealthChecks().AddCheck<QueueHealthCheck>(\"queue\")",["services.AddSingleton<QueueHealthCheck>() únicamente, sin más configuración","services.MapHealthChecks<QueueHealthCheck>()","app.UseHealthCheck<QueueHealthCheck>(\"queue\")"],2,"AddHealthChecks() habilita el sistema de health checks y AddCheck<T>(name) registra una implementación de IHealthCheck bajo un nombre identificable en el reporte."),
  sqr("¿Qué línea expone el endpoint HTTP donde se pueden consultar los health checks configurados?","app.MapHealthChecks(\"/health\")",["app.UseHealthEndpoint()","app.Run(\"/health\")","services.ExposeHealthChecks(\"/health\")"],3,"MapHealthChecks(path) registra el middleware/endpoint que, al ser consultado, ejecuta todos los health checks registrados y retorna un resultado agregado."),
  sqr("¿Qué diferencia práctica hay entre un readiness probe y un liveness probe en Kubernetes, en relación con health checks?","El liveness probe indica si el proceso sigue vivo (si falla, Kubernetes reinicia el pod); el readiness probe indica si la aplicación está lista para recibir tráfico (si falla, Kubernetes deja de enrutarle peticiones sin reiniciarla)",["Son exactamente lo mismo, con nombres distintos por convención","El liveness probe siempre revisa la base de datos; el readiness probe nunca lo hace","El readiness probe reinicia el pod; el liveness probe solo registra logs"],0,"Kubernetes distingue ambos conceptos: liveness detecta procesos colgados (acción: reiniciar), readiness detecta si la app puede atender tráfico ahora mismo (acción: quitar del balanceo temporalmente), y ambos suelen mapearse a distintos endpoints de health checks con tags distintos."),
  sqr("¿Qué método de extensión permite agregar un health check predefinido para verificar la conectividad de un DbContext de EF Core?","AddDbContextCheck<AppDbContext>()",["AddCheck<AppDbContext>() sin ningún paquete adicional","AddEfCoreCheck()","AddDatabaseHealthy<AppDbContext>()"],1,"AddDbContextCheck<TContext>() (del paquete Microsoft.Extensions.Diagnostics.HealthChecks.EntityFrameworkCore) agrega un health check que intenta conectarse usando el DbContext indicado."),
  sqr("En QueueHealthCheck, ¿qué representa el parámetro 'HealthCheckContext context' que recibe CheckHealthAsync?","Contexto de ejecución del health check, incluyendo el HealthCheckRegistration asociado (nombre, tags, umbral de fallo configurado)",["El HttpContext completo de la petición que originó la consulta","Una conexión abierta a la base de datos","El resultado de otro health check ejecutado previamente"],2,"HealthCheckContext expone información sobre cómo fue registrado el check (nombre, tags, failure status configurado), útil si el check necesita comportarse distinto según su configuración."),
  sqr("¿Qué trampa de entrevista es común sobre 'un health check que siempre retorna Healthy sin verificar nada real'?","Da una falsa sensación de seguridad: el orquestador seguirá enviando tráfico o considerará la instancia sana aunque una dependencia crítica (base de datos, cola) esté realmente caída",["Es la forma recomendada de implementar health checks en producción","No tiene ningún efecto práctico, los health checks son solo decorativos","Kubernetes ignora los health checks que siempre retornan Healthy"],3,"Un health check debe verificar realmente el estado de la dependencia (por ejemplo, intentando una consulta ligera); si siempre retorna Healthy sin verificar nada, pierde su propósito de detectar fallos reales."),
  sqr("Si se registran dos health checks (uno de base de datos, otro de la cola) y solo el de la cola falla con Unhealthy, ¿qué estado agregado retorna típicamente el endpoint /health por defecto?","Unhealthy en conjunto: por defecto el estado general refleja el peor resultado individual entre todos los checks registrados",["Healthy, porque la mayoría de los checks (la base de datos) pasó","Degraded siempre, sin importar cuántos checks fallen","El endpoint ignora los checks fallidos y no los reporta"],0,"El comportamiento por defecto del middleware de health checks agrega el estado combinando todos los resultados; si al menos uno reporta Unhealthy, el estado general de la respuesta también es Unhealthy."),
  sqr("¿Qué relación tienen los health checks con los background services vistos en este módulo?","Un health check puede verificar el estado de un worker o de la cola que consume (por ejemplo, si el canal sigue procesando o si el worker se detuvo), exponiendo esa información al orquestador",["No tienen ninguna relación, health checks solo aplican a bases de datos","Los health checks reemplazan por completo la necesidad de BackgroundService","Un health check solo puede ejecutarse dentro de un BackgroundService, nunca de forma independiente"],1,"Los health checks son una herramienta de observabilidad operacional que puede reflejar el estado de cualquier dependencia interna, incluyendo colas y workers en segundo plano, ayudando a detectar si dejaron de procesar trabajo."),
 ]},
 {id:"n07-domain-events",title:"Eventos de dominio",summary:"Un evento de dominio representa algo relevante que ya ocurrió dentro del modelo de negocio (por ejemplo, 'OrderCreated'), levantado por la propia entidad/aggregate root. Se recolectan en una lista dentro de la entidad y se despachan típicamente después de confirmar la transacción (tras SaveChanges), para garantizar que solo se notifiquen eventos de cambios que realmente se persistieron; despacharlos antes de confirmar arriesga notificar un evento cuyo cambio luego se revierte por un fallo o rollback.",code:`public abstract class Entity
{
    private readonly List<object> _domainEvents = new();
    public IReadOnlyList<object> DomainEvents => _domainEvents.AsReadOnly();
    protected void AddDomainEvent(object domainEvent) => _domainEvents.Add(domainEvent);
    public void ClearDomainEvents() => _domainEvents.Clear();
}

public record OrderCreatedEvent(int OrderId, int CustomerId);

public class Order : Entity
{
    public int Id { get; private set; }
    public int CustomerId { get; private set; }

    public Order(int customerId)
    {
        CustomerId = customerId;
        AddDomainEvent(new OrderCreatedEvent(Id, CustomerId)); // se levanta al crear, no se despacha aún
    }
}

// Después de _context.SaveChangesAsync() exitoso, un interceptor/UnitOfWork despacha
// cada evento acumulado (por ejemplo, publicándolo con MediatR) y luego llama ClearDomainEvents().`,questions:[
  sqr("¿Dónde se origina (levanta) un evento de dominio como OrderCreatedEvent?","Dentro de la propia entidad/aggregate root del Dominio, como parte de su lógica de negocio",["En el controller de la Web API","En el middleware de autenticación","En la capa de configuración de appsettings.json"],0,"Los eventos de dominio nacen del propio modelo de negocio: la entidad los agrega a su lista interna cuando ocurre algo relevante, como parte de su comportamiento encapsulado."),
  sqr("¿Por qué en el ejemplo Order acumula OrderCreatedEvent en una lista en vez de publicarlo inmediatamente dentro del constructor?","Porque publicar el evento antes de confirmar la transacción arriesga notificar un cambio que podría revertirse si SaveChanges falla; se prefiere despachar después de confirmar la persistencia",["Porque los constructores en C# no pueden llamar a otros métodos","Porque MediatR no permite publicar eventos desde una entidad de Dominio","Porque las listas son más eficientes que llamar directamente a un método Publish"],1,"Despachar el evento solo tras un SaveChanges exitoso garantiza que los suscriptores reaccionen únicamente a cambios que efectivamente quedaron persistidos, evitando notificaciones sobre transacciones que se revirtieron."),
  sqr("¿Qué patrón habitual se usa para despachar automáticamente los eventos acumulados justo después de un SaveChangesAsync exitoso en EF Core?","Un interceptor de SaveChanges (o lógica en el Unit of Work) que recorre las entidades rastreadas, recolecta sus DomainEvents y los publica (por ejemplo, con MediatR.Publish), limpiando la lista después",["Un atributo [DispatchOnSave] aplicado directamente sobre la entidad","No existe forma de hacerlo automáticamente en EF Core","Sobrescribir el operador new de la entidad"],2,"Es común usar un SaveChangesInterceptor de EF Core (o lógica equivalente en Unit of Work) que, tras confirmar los cambios, recorre las entidades con eventos pendientes y los despacha antes de limpiar la colección."),
  sqr("¿Qué método de la clase Entity del ejemplo evita que un mismo evento se despache dos veces si SaveChanges se llama nuevamente más tarde?","ClearDomainEvents(), que vacía la lista interna después de despachar los eventos",["AddDomainEvent(), llamado una segunda vez","DomainEvents, que es de solo lectura","No hay forma de evitarlo, siempre se duplican los eventos"],3,"Tras despachar los eventos acumulados, se debe llamar ClearDomainEvents() para vaciar la lista; de lo contrario, un SaveChanges posterior podría volver a despachar los mismos eventos."),
  sqr("¿Qué diferencia hay entre un evento de dominio (domain event) y un comando (command) visto en el módulo anterior?","Un evento de dominio describe algo que YA ocurrió en el pasado (hecho consumado) y puede tener cero o varios manejadores; un command representa una intención de acción futura y típicamente tiene un único handler",["Son sinónimos exactos, solo cambia el nombre de la clase","Un evento de dominio siempre requiere una base de datos distinta a la del command","Los commands no pueden manejarse con MediatR, solo los eventos sí"],0,"Los commands expresan intención ('crea este pedido') con un handler específico; los eventos expresan hechos consumados ('el pedido ya fue creado') que pueden interesar a múltiples suscriptores distintos."),
  sqr("¿Qué riesgo existe si un evento de dominio se despacha (y sus efectos secundarios se ejecutan, como enviar un email) ANTES de confirmar la transacción con SaveChanges, y luego la transacción falla?","Se notificaría un evento (y se ejecutarían sus efectos, como enviar un email de confirmación) sobre un cambio que en realidad nunca se persistió, generando inconsistencia entre lo notificado y el estado real del sistema",["Ningún riesgo, EF Core revierte automáticamente los efectos secundarios de los suscriptores","El evento simplemente no se ejecutaría en absoluto","La transacción se completaría automáticamente para evitar el problema"],1,"Publicar eventos antes de confirmar la persistencia puede provocar efectos secundarios (emails, integraciones externas) basados en un estado que después se revierte, generando inconsistencias difíciles de corregir."),
  sqr("¿Qué relación tiene MediatR (visto en el módulo anterior) con el despacho de eventos de dominio?","MediatR suele usarse como mecanismo para publicar los eventos de dominio recolectados (vía INotification/mediator.Publish) a sus handlers correspondientes dentro del mismo proceso",["MediatR es incompatible con eventos de dominio por diseño","Los eventos de dominio requieren obligatoriamente una cola externa, nunca MediatR","MediatR solo puede manejar commands, nunca notificaciones/eventos"],0,"Como INotification de MediatR permite múltiples handlers para el mismo mensaje, es una elección común para despachar eventos de dominio a distintos suscriptores en memoria."),
  sqr("¿Qué trampa de entrevista es común sobre 'dónde vive la lógica que reacciona a un evento de dominio' (por ejemplo, enviar un email de bienvenida tras OrderCreatedEvent)?","Esa lógica de reacción normalmente vive en un handler de la capa de Aplicación (o Infraestructura), NO dentro de la propia entidad de Dominio, que solo se encarga de levantar el evento",["La entidad de Dominio debe enviar el email directamente desde su constructor","No debe existir ninguna reacción a los eventos de dominio, solo se registran como log","Los eventos de dominio solo pueden reaccionar modificando la misma entidad que los generó"],3,"La entidad de Dominio permanece libre de dependencias externas (como un servicio de email); simplemente levanta el evento, y un handler en una capa externa se encarga de reaccionar con el efecto secundario correspondiente."),
  sqr("En el ejemplo, ¿qué tipo de dato usa AddDomainEvent para almacenar los eventos en la lista _domainEvents?","object, ya que la lista debe poder contener distintos tipos de eventos de dominio (no solo OrderCreatedEvent)",["Exclusivamente string con el nombre del evento serializado","IHostedService","Task, porque los eventos siempre son asíncronos"],1,"Como una entidad puede levantar distintos tipos de eventos a lo largo de su vida, la colección se tipa de forma genérica (object, o una interfaz marcador común como IDomainEvent) para admitir cualquiera de ellos."),
  sqr("¿Qué característica distingue a OrderCreatedEvent como 'record' en el ejemplo, en el contexto de un evento de dominio?","Es un objeto inmutable de solo datos que describe un hecho ya ocurrido (el pedido con Id y CustomerId ya fue creado), sin comportamiento propio",["Puede modificar directamente el estado de la entidad Order después de ser creado","Reemplaza la necesidad de que Order tenga un Id","Debe heredar obligatoriamente de BackgroundService"],0,"Modelar el evento como un record inmutable refuerza su naturaleza de 'hecho consumado': una vez creado, describe datos fijos sobre algo que ya sucedió, sin lógica mutable asociada."),
 ]},
 {id:"n07-retries",title:"Reintentos",summary:"Un reintento (retry) vuelve a ejecutar una operación que falló por una causa transitoria (timeout de red, servicio temporalmente no disponible), en vez de propagar el error inmediatamente. La estrategia recomendada es backoff exponencial con jitter: el tiempo de espera entre reintentos crece exponencialmente (1s, 2s, 4s...) y se le suma una variación aleatoria (jitter) para evitar que múltiples clientes reintenten exactamente al mismo tiempo (efecto 'thundering herd'). Polly es la biblioteca estándar de .NET para implementar políticas de reintento, timeout y circuit breaker de forma declarativa.",code:`// Política de reintento con backoff exponencial usando Polly
var retryPolicy = Policy
    .Handle<HttpRequestException>()
    .WaitAndRetryAsync(
        retryCount: 3,
        sleepDurationProvider: attempt => TimeSpan.FromSeconds(Math.Pow(2, attempt))
                                          + TimeSpan.FromMilliseconds(Random.Shared.Next(0, 500)));

await retryPolicy.ExecuteAsync(async () =>
{
    var response = await httpClient.GetAsync("https://api.externa.com/pedidos");
    response.EnsureSuccessStatusCode();
});`,questions:[
  sqr("¿Qué tipo de fallo es candidato a resolverse con un reintento automático?","Un fallo transitorio, como un timeout de red o un servicio temporalmente no disponible, que probablemente se resuelva por sí solo al intentar de nuevo",["Un error de validación de datos de entrada del usuario","Una excepción de referencia nula (NullReferenceException) por un bug de código","Un error de compilación"],0,"Los reintentos tienen sentido para fallos transitorios (red, disponibilidad momentánea); errores de lógica, validación o bugs de código no se resuelven reintentando la misma operación."),
  sqr("¿Qué es el 'backoff exponencial' en una estrategia de reintentos?","Un patrón donde el tiempo de espera entre cada reintento sucesivo crece exponencialmente (por ejemplo 1s, 2s, 4s, 8s...) en vez de mantenerse constante",["Reintentar inmediatamente sin ninguna espera entre intentos","Reducir el tiempo de espera en cada reintento sucesivo","Reintentar un número infinito de veces sin límite"],1,"El backoff exponencial da tiempo creciente para que la causa transitoria del fallo se resuelva, evitando saturar aún más un servicio ya con problemas."),
  sqr("¿Qué problema resuelve agregar 'jitter' (variación aleatoria) al tiempo de espera entre reintentos?","Evita que múltiples clientes que fallaron al mismo tiempo reintenten simultáneamente, generando un pico de carga sincronizado (efecto 'thundering herd') justo cuando el servicio se está recuperando",["Hace que los reintentos sean instantáneos sin ninguna espera","Elimina por completo la necesidad de un límite máximo de reintentos","Convierte los reintentos síncronos en asíncronos automáticamente"],2,"Sin jitter, muchos clientes que fallaron a la vez reintentarían exactamente en el mismo instante, generando una nueva ráfaga de carga sincronizada; el jitter dispersa esos reintentos en el tiempo."),
  sqr("¿Qué biblioteca es el estándar de facto en .NET para implementar políticas de reintento, timeout y circuit breaker de forma declarativa?","Polly",["System.Threading.Channels","Entity Framework Core","MediatR"],3,"Polly ofrece una API fluida (Policy.Handle<TException>().WaitAndRetryAsync(...), CircuitBreakerAsync(...), TimeoutAsync(...)) ampliamente adoptada en el ecosistema .NET para resiliencia."),
  sqr("En el código de ejemplo, ¿qué controla el parámetro 'retryCount: 3' de WaitAndRetryAsync?","El número máximo de reintentos después del intento original antes de dejar que la excepción se propague",["El número de milisegundos de espera fijo entre cada intento","El número de hilos usados para ejecutar la operación en paralelo","El número de servicios externos a los que se les hace la petición"],0,"retryCount limita cuántas veces se reintentará la operación tras el primer fallo; si se agotan los reintentos sin éxito, la excepción finalmente se propaga al llamador."),
  sqr("¿Qué diferencia hay entre un retry y un circuit breaker como estrategias de resiliencia?","Un retry vuelve a intentar la misma operación tras un fallo; un circuit breaker deja de intentar por completo durante un periodo, tras detectar una cantidad de fallos consecutivos, para no seguir golpeando un servicio ya caído",["Son exactamente el mismo concepto con nombres distintos","Un circuit breaker siempre reintenta más veces que un retry simple","Un retry nunca puede combinarse con un circuit breaker en la misma política"],1,"El circuit breaker 'abre el circuito' tras detectar fallos repetidos, rechazando llamadas inmediatamente durante un tiempo, en vez de seguir reintentando contra un servicio que probablemente seguirá fallando; ambos patrones se combinan frecuentemente en Polly."),
  sqr("¿Por qué reintentar automáticamente una operación de pago (por ejemplo, 'cobrar $100 a una tarjeta') sin ninguna precaución adicional es peligroso?","Porque si el primer intento sí tuvo éxito en el servidor pero la respuesta se perdió (timeout de red en el camino de vuelta), un reintento ciego podría cobrar dos veces al cliente",["Porque los pagos nunca pueden fallar por causas transitorias","Porque Polly no soporta operaciones que involucren dinero","Porque los reintentos siempre son instantáneos y no dan tiempo a que el pago se procese"],2,"Este es exactamente el motivo por el que los reintentos necesitan operaciones idempotentes (tema del siguiente paso): sin idempotencia, un reintento tras un fallo ambiguo puede duplicar un efecto ya aplicado."),
  sqr("¿Qué trampa de entrevista es común sobre 'reintentar siempre resuelve los problemas de un sistema distribuido'?","Los reintentos sin límite ni backoff pueden empeorar una situación de sobrecarga, generando aún más tráfico hacia un servicio que ya está fallando por estar sobrecargado",["Reintentar siempre es gratis y nunca tiene efectos secundarios negativos","Cuantos más reintentos, mejor, sin ningún límite recomendado","Los reintentos eliminan por completo la necesidad de monitoreo"],0,"Una política de reintentos mal diseñada (sin límite, sin backoff, sin jitter) puede amplificar una caída parcial en una caída total, por lo que se combina con límites, backoff exponencial y a veces circuit breakers."),
  sqr("¿Qué haría 'Math.Pow(2, attempt)' en la política del ejemplo para attempt = 1, 2 y 3 (antes de sumar el jitter)?","2, 4 y 8 segundos de espera base respectivamente, creciendo exponencialmente con cada intento",["1, 1 y 1 segundos, siempre constante","2, 3 y 4 segundos, creciendo de forma lineal","8, 4 y 2 segundos, decreciendo con cada intento"],1,"Math.Pow(2, attempt) produce 2^1=2, 2^2=4, 2^3=8, ilustrando el crecimiento exponencial característico del backoff antes de sumar la variación aleatoria del jitter."),
  sqr("¿En qué tipo de operación tiene MENOS sentido aplicar un reintento automático sin análisis adicional?","Una operación que ya falló por un error de validación de negocio (por ejemplo, stock insuficiente), ya que reintentar la misma operación fallará exactamente igual todas las veces",["Una llamada HTTP que falló por un timeout de red puntual","Una consulta a una base de datos que falló por una desconexión momentánea","Una llamada a un servicio externo que respondió con un error 503 (Service Unavailable)"],2,"Los errores de negocio/validación no son transitorios: la misma entrada producirá el mismo resultado en cada reintento, por lo que reintentar ciegamente no soluciona nada y solo desperdicia recursos."),
 ]},
 {id:"n07-idempotency",title:"Idempotencia",summary:"Una operación es idempotente si ejecutarla múltiples veces con los mismos datos produce el mismo resultado final que ejecutarla una sola vez, sin efectos secundarios duplicados. Es crítica quando se combinan reintentos con operaciones que tienen efectos secundarios (crear un pedido, cobrar un pago): sin idempotencia, un reintento tras una respuesta perdida puede duplicar la operación. Una técnica común es la idempotency key: el cliente envía un identificador único por operación, y el servidor recuerda las claves ya procesadas para no repetir el efecto si la misma clave llega de nuevo.",code:`public class PaymentsController : ControllerBase
{
    private readonly IMemoryCache _processedKeys;
    private readonly IPaymentService _payments;

    [HttpPost("charge")]
    public async Task<IActionResult> Charge(
        [FromHeader(Name = "Idempotency-Key")] string idempotencyKey,
        ChargeRequest request)
    {
        if (_processedKeys.TryGetValue(idempotencyKey, out ChargeResult? existing))
            return Ok(existing); // ya procesado antes: se devuelve el mismo resultado, sin cobrar de nuevo

        var result = await _payments.ChargeAsync(request);
        _processedKeys.Set(idempotencyKey, result, TimeSpan.FromHours(24));
        return Ok(result);
    }
}`,questions:[
  sqr("¿Qué significa que una operación sea idempotente?","Ejecutarla una o varias veces con los mismos datos produce el mismo resultado final, sin duplicar efectos secundarios",["Que siempre se ejecuta de forma asíncrona","Que nunca puede fallar bajo ninguna circunstancia","Que se ejecuta exactamente una sola vez en la vida del sistema, sin excepción"],0,"La idempotencia garantiza que repetir la misma operación (por ejemplo, por un reintento) no cause un efecto acumulado distinto al de ejecutarla una única vez."),
  sqr("Según la semántica HTTP estándar, ¿cuál de estos verbos se considera naturalmente idempotente por definición?","PUT (reemplazar un recurso completo con un valor dado)",["POST, para crear un nuevo recurso cada vez","PATCH parcial con un contador que incrementa un valor","Ninguno de los verbos HTTP es idempotente por definición"],1,"PUT y DELETE se consideran idempotentes por la especificación HTTP: aplicarlos varias veces con los mismos datos deja el recurso en el mismo estado final. POST, en cambio, típicamente crea un nuevo recurso en cada llamada, por lo que NO es naturalmente idempotente."),
  sqr("¿Por qué un endpoint POST para 'crear un pedido' normalmente NO es idempotente por sí solo?","Porque cada llamada a POST, por diseño semántico, tiende a crear un nuevo recurso; ejecutarlo dos veces con los mismos datos crearía dos pedidos distintos",["Porque POST no puede recibir un cuerpo (body) en la petición","Porque POST siempre requiere autenticación, a diferencia de otros verbos","Porque los pedidos nunca pueden crearse mediante una API HTTP"],2,"A diferencia de PUT (que reemplaza un recurso identificado), POST habitualmente genera un recurso nuevo en cada invocación, por lo que reintentar un POST sin ninguna protección adicional puede duplicar la creación."),
  sqr("¿Qué es una 'idempotency key' y qué problema resuelve en el ejemplo de PaymentsController?","Un identificador único generado por el cliente para una operación específica; el servidor lo usa para detectar si esa misma operación ya fue procesada antes y evitar repetir el cobro",["Una clave de cifrado usada para encriptar la base de datos","Un token de autenticación que reemplaza a JWT","Un identificador exclusivo de la sesión HTTP, generado automáticamente por el navegador"],0,"El cliente envía la misma Idempotency-Key en un reintento de la misma operación lógica; el servidor recuerda las claves ya procesadas y devuelve el resultado guardado en vez de repetir el efecto (el cobro)."),
  sqr("En el ejemplo, ¿qué ocurre si Charge() se invoca dos veces con la MISMA Idempotency-Key debido a un reintento del cliente por un timeout de red?","La segunda invocación encuentra la clave ya registrada en _processedKeys y retorna el resultado guardado, sin volver a llamar a _payments.ChargeAsync (evitando un doble cobro)",["Se cobra al cliente dos veces, una por cada invocación","La segunda invocación lanza una excepción no controlada siempre","El sistema ignora ambas invocaciones y no cobra nada"],1,"El chequeo 'if (_processedKeys.TryGetValue(idempotencyKey, ...))' es justamente el mecanismo que evita reprocesar (y volver a cobrar) una operación que ya se ejecutó con esa misma clave."),
  sqr("¿Qué relación tiene la idempotencia con los reintentos vistos en el paso anterior?","Los reintentos automáticos son seguros de aplicar solo si la operación reintentada es idempotente; de lo contrario, un reintento puede duplicar un efecto secundario ya aplicado (como un cobro o el envío de un email)",["No tienen ninguna relación entre sí","La idempotencia solo aplica a operaciones de solo lectura, nunca a reintentos de escritura","Los reintentos siempre garantizan idempotencia automáticamente, sin necesidad de diseño adicional"],2,"Un reintento repite una operación que pudo haber tenido éxito parcial o total sin que el cliente lo supiera (por ejemplo, por una respuesta perdida); sin idempotencia, ese reintento puede duplicar el efecto real de la operación."),
  sqr("¿Qué trampa de entrevista es común sobre asumir que 'agregar reintentos automáticos siempre es seguro'?","Agregar reintentos a una operación con efectos secundarios no idempotente (como cobrar un pago o enviar un email) puede introducir duplicados que antes no existían, empeorando la confiabilidad en vez de mejorarla",["Los reintentos siempre son seguros sin importar el tipo de operación","La idempotencia es responsabilidad exclusiva del cliente, nunca del servidor","Solo las operaciones de lectura necesitan ser idempotentes"],0,"Antes de agregar reintentos automáticos a una operación, hay que verificar que sea idempotente (naturalmente, o mediante técnicas como idempotency keys); de lo contrario los reintentos introducen el riesgo de duplicación."),
  sqr("¿Qué operación es naturalmente idempotente sin necesitar ninguna técnica adicional: 'establecer el estado de un pedido a Cancelado' (PedidoEstado = Cancelado) o 'incrementar el stock en 5 unidades' (Stock += 5)?","'Establecer el estado a Cancelado', porque asignar un valor fijo produce el mismo resultado final sin importar cuántas veces se ejecute",["'Incrementar el stock en 5 unidades', porque los números siempre son idempotentes","Ambas son igualmente idempotentes sin ninguna diferencia","Ninguna de las dos operaciones puede ser idempotente jamás"],1,"Asignar un valor absoluto (PedidoEstado = Cancelado) dos veces deja el mismo resultado; en cambio, 'Stock += 5' ejecutado dos veces suma 10 en total, un efecto claramente distinto a ejecutarlo una sola vez, por lo que NO es idempotente por sí sola."),
  sqr("¿Cuánto tiempo deberían recordarse típicamente las idempotency keys ya procesadas, como TimeSpan.FromHours(24) en el ejemplo?","Un tiempo razonable acotado (horas o días), suficiente para cubrir la ventana probable de reintentos del cliente, sin necesidad de guardarlas indefinidamente para siempre",["Para siempre, sin ningún límite de expiración, ya que deben persistir por años","Nunca deben guardarse; verificar duplicados en cada petición basta sin memoria alguna","Exactamente 1 milisegundo, para minimizar el uso de memoria"],2,"Guardar las claves indefinidamente no es práctico; se elige una ventana de tiempo razonable que cubra los reintentos esperables del cliente, equilibrando protección contra duplicados y uso de recursos (memoria/almacenamiento)."),
  sqr("¿Qué diferencia hay entre validar la idempotencia con una idempotency key enviada por el cliente y simplemente confiar en que el ID autogenerado del recurso (por ejemplo, el Id de un Order) evite duplicados?","La idempotency key la define el cliente ANTES de saber si la operación tendrá éxito, permitiendo detectar reintentos de la MISMA intención; un Id autogenerado por el servidor se crea DESPUÉS, por lo que cada intento fallido/reintentado generaría un Id distinto y no evitaría la duplicación",["Son exactamente equivalentes, cualquiera de las dos resuelve el problema igual de bien","Un Id autogenerado siempre es mejor porque es más corto que una idempotency key","La idempotency key solo sirve para operaciones de lectura (GET), nunca de escritura"],0,"Como el Id del recurso se genera como resultado de la operación, no sirve para detectar que dos peticiones distintas eran en realidad reintentos de la misma intención; la idempotency key, generada por el cliente antes del intento, sí permite esa correlación."),
 ]},
 {id:"n07-challenge",title:"Reto de workers",summary:"Reto integrador del módulo: combina IHostedService/BackgroundService, una cola in-process con Channel<T>, health checks, eventos de dominio, reintentos con backoff exponencial e idempotencia en un flujo de procesamiento de pedidos en segundo plano.",code:`public class OrderProcessingWorker : BackgroundService
{
    private readonly IOrderQueue _queue;
    private readonly IPaymentService _payments;
    private readonly ILogger<OrderProcessingWorker> _logger;
    private static readonly AsyncRetryPolicy RetryPolicy = Policy
        .Handle<HttpRequestException>()
        .WaitAndRetryAsync(3, attempt => TimeSpan.FromSeconds(Math.Pow(2, attempt)));

    public OrderProcessingWorker(IOrderQueue queue, IPaymentService payments, ILogger<OrderProcessingWorker> logger)
    { _queue = queue; _payments = payments; _logger = logger; }

    protected override async Task ExecuteAsync(CancellationToken stoppingToken)
    {
        await foreach (var order in _queue.ReadAllAsync(stoppingToken))
        {
            await RetryPolicy.ExecuteAsync(ct => _payments.ChargeAsync(order.PaymentIdempotencyKey, order.Total, ct), stoppingToken);
            _logger.LogInformation("Pedido {OrderId} procesado", order.Id);
        }
    }
}`,questions:[
  sqr("¿Qué clase base implementa OrderProcessingWorker para ejecutarse en segundo plano dentro del mismo proceso?","BackgroundService",["IHealthCheck","IUnitOfWork","IOptionsMonitor<T>"],0,"BackgroundService es la clase abstracta que provee ExecuteAsync como plantilla para trabajo continuo en segundo plano, administrada por el Generic Host."),
  sqr("¿Qué patrón representa '_queue.ReadAllAsync(stoppingToken)' consumido con 'await foreach' dentro de ExecuteAsync?","Un consumidor asíncrono de una cola in-process (Channel<T>), que procesa cada pedido a medida que llega y se detiene de forma cooperativa al cancelarse stoppingToken",["Una consulta directa y síncrona a una base de datos relacional","Un mecanismo de sondeo con Thread.Sleep en un bucle infinito","Una llamada RPC a un microservicio externo"],1,"El worker consume la cola en memoria expuesta por IOrderQueue (basada en Channel<T>) de forma asíncrona, deteniéndose limpiamente cuando el token de cancelación se activa durante el apagado."),
  sqr("¿Por qué RetryPolicy.ExecuteAsync envuelve la llamada a _payments.ChargeAsync en vez de dejar que una HttpRequestException se propague directamente?","Porque un fallo de red al cobrar puede ser transitorio; reintentar con backoff exponencial (2s, 4s, 8s) da oportunidad de que la causa se resuelva antes de dejar el pedido como fallido",["Porque ChargeAsync nunca puede fallar sin la política de reintentos","Porque Polly es obligatorio para poder usar async/await en el worker","Porque el worker no puede manejar excepciones de ningún otro modo"],2,"Un timeout o error transitorio al llamar al servicio de pagos es un candidato clásico para reintentos con backoff, en vez de descartar el pedido tras el primer fallo."),
  sqr("¿Qué evita que un reintento de RetryPolicy sobre ChargeAsync cobre dos veces al cliente si el primer intento sí tuvo éxito en el servidor pero la confirmación se perdió por un problema de red?","Que ChargeAsync use una idempotency key (order.PaymentIdempotencyKey) para que el servicio de pagos reconozca un reintento de la misma operación y no repita el cobro",["Que RetryPolicy tenga un límite de 3 intentos, sin relación con duplicar el cobro","Que ExecuteAsync use 'await foreach' en vez de un bucle 'for' tradicional","Que BackgroundService implemente IHostedService internamente"],0,"El parámetro order.PaymentIdempotencyKey es justamente la técnica de idempotencia que permite que un reintento seguro no duplique el efecto de cobrar, incluso si el intento anterior sí tuvo éxito pero la respuesta se perdió."),
  sqr("Si se agrega un health check que verifica cuántos elementos hay pendientes en la cola de OrderProcessingWorker, ¿qué propósito operacional cumpliría?","Permitir que un orquestador (por ejemplo Kubernetes) detecte que la cola está creciendo sin ser procesada (el worker podría estar caído o muy lento) y actúe en consecuencia",["Reemplazar la necesidad de que el worker procese pedidos","Cobrar automáticamente los pedidos pendientes sin pasar por ChargeAsync","Evitar que se levanten nuevos eventos de dominio"],1,"Un health check que refleje el tamaño de la cola o el estado del worker es información valiosa de observabilidad: permite detectar y reaccionar ante un procesamiento estancado antes de que se acumule un backlog grande."),
  sqr("Si el propio Order levantó un evento de dominio OrderCreatedEvent al crearse, ¿en qué momento del flujo completo (creación del pedido → encolado → procesamiento por el worker) sería más apropiado despacharlo, según lo visto en este módulo?","Justo después de que la creación del pedido se confirme exitosamente en la base de datos (tras SaveChanges), no antes",["Dentro del constructor de Order, de forma síncrona e inmediata","Dentro de ExecuteAsync del worker, después de cobrar el pago","Nunca, los eventos de dominio no deben despacharse en un sistema con colas"],0,"Igual que en el paso de eventos de dominio, el despacho debe ocurrir tras confirmar la persistencia, para no notificar sobre un pedido que en definitiva no llegó a crearse si la transacción falla."),
  sqr("¿Qué ocurre con los pedidos que aún están en la cola in-process (Channel<T>) si el proceso de la aplicación se reinicia abruptamente antes de que OrderProcessingWorker los procese?","Se pierden, porque Channel<T> es una cola en memoria sin persistencia; para garantizar que sobrevivan a un reinicio se necesitaría una cola externa durable",["Se recuperan automáticamente al reiniciar gracias a StopAsync de BackgroundService","Kubernetes los reprocesa automáticamente sin configuración adicional","Se guardan automáticamente en la base de datos por EF Core"],1,"Esta es una limitación real de usar Channel<T> como cola: al ser puramente en memoria, cualquier elemento no procesado se pierde si el proceso termina inesperadamente, a diferencia de una cola externa persistente."),
  sqr("¿Qué haría StopAsync (heredado de BackgroundService) cuando el host solicita el apagado mientras OrderProcessingWorker está procesando un pedido dentro de RetryPolicy.ExecuteAsync?","Cancela stoppingToken y espera (con un tiempo límite) a que la tarea de ExecuteAsync termine de forma ordenada, lo que puede interrumpir el 'await foreach' y las esperas internas de Polly que reciben ese mismo token",["Mata el proceso inmediatamente sin dar tiempo a terminar nada","Reinicia automáticamente el pedido en curso desde cero en otro hilo","Ignora la señal de apagado hasta que se procesen todos los pedidos restantes de la cola, sin límite de tiempo"],0,"El comportamiento estándar de BackgroundService.StopAsync es señalar la cancelación y esperar con un timeout; como stoppingToken se propaga al 'await foreach' y a RetryPolicy.ExecuteAsync, todo el flujo puede cortarse de forma cooperativa."),
  sqr("¿Qué principio del módulo anterior (SOLID) se refleja en que OrderProcessingWorker dependa de IOrderQueue e IPaymentService inyectados, en vez de instanciar Channel<Order> y un cliente HTTP concreto directamente?","DIP (Dependency Inversion Principle): el worker depende de abstracciones, facilitando pruebas y sustitución de implementaciones",["OCP, porque el worker no puede extenderse nunca","LSP, porque IOrderQueue no tiene subtipos","SRP, porque ExecuteAsync solo tiene una línea de código"],0,"Igual que en el módulo de arquitectura, inyectar interfaces (IOrderQueue, IPaymentService) en vez de depender de implementaciones concretas es la aplicación práctica de DIP, y facilita testear el worker con dobles de prueba."),
  sqr("¿Qué combinación de conceptos del módulo se refleja mejor en OrderProcessingWorker (BackgroundService que consume un Channel<T>, reintenta con Polly usando una idempotency key, y registra logs)?","BackgroundService + colas in-process + reintentos con backoff exponencial + idempotencia + observabilidad, todos colaborando en el procesamiento confiable de un pedido en segundo plano",["Solo health checks, el resto de conceptos no participan en este código","Solo eventos de dominio, ya que Order los levantó al crearse","Ninguno de los conceptos del módulo, es código genérico sin relación con ellos"],1,"El worker del reto combina explícitamente varias piezas del módulo: ejecución en segundo plano, consumo de una cola, resiliencia ante fallos transitorios con reintentos, protección contra duplicados con idempotencia, y logging para diagnosticar el procesamiento."),
 ]},
 {id:"n08-allocations-gc",title:"Allocations y GC",summary:"El Garbage Collector (GC) de .NET usa un heap generacional: los objetos nuevos se crean en Gen0, que se recolecta con frecuencia y a bajo costo porque la mayoría de objetos mueren jóvenes. Los objetos que sobreviven pasan a Gen1 y luego a Gen2, cuya recolección es más costosa porque implica revisar más memoria. Objetos grandes (>= 85,000 bytes) van al Large Object Heap (LOH), que se recolecta junto con Gen2 y es propenso a fragmentación. Minimizar allocations innecesarias (boxing, closures, concatenación de strings en bucles) reduce la presión sobre el GC.",code:`// Genera muchas allocations innecesarias en el heap
public string BuildReport(List<int> values)
{
    string result = "";
    foreach (var v in values)
        result += v.ToString() + ", "; // cada += crea un nuevo string en el heap
    return result;
}

// Versión con menos presión sobre el GC
public string BuildReportEfficient(List<int> values)
{
    var sb = new StringBuilder();
    foreach (var v in values)
        sb.Append(v).Append(", "); // reutiliza el buffer interno, muchas menos allocations
    return sb.ToString();
}`,questions:[
  sqr("¿Por qué la recolección de Gen0 es generalmente barata comparada con la de Gen2?","Porque Gen0 es pequeña y la mayoría de los objetos allí mueren jóvenes, por lo que recolectarla implica revisar poca memoria y copiar pocos sobrevivientes",["Porque Gen0 nunca contiene objetos vivos, solo memoria libre","Porque Gen0 se recolecta una sola vez en toda la vida del proceso","Porque Gen0 no usa el algoritmo generacional del GC"],0,"La hipótesis generacional del GC asume que la mayoría de los objetos mueren jóvenes; Gen0 es pequeña y se limpia frecuentemente a bajo costo, mientras Gen2 requiere revisar mucha más memoria."),
  sqr("¿Qué caracteriza a un objeto que termina en el Large Object Heap (LOH)?","Tiene un tamaño igual o mayor a 85,000 bytes aproximadamente, y se recolecta junto con Gen2",["Cualquier objeto que implemente IDisposable","Solo los objetos de tipo string, sin importar su tamaño","Objetos creados dentro de un using statement"],1,"El umbral aproximado de 85,000 bytes determina si un objeto se asigna en el LOH en lugar del heap normal; el LOH se recolecta junto a Gen2 y es más propenso a fragmentación porque tradicionalmente no se compacta en cada recolección."),
  sqr("En BuildReport(), ¿por qué 'result += v.ToString() + \", \"' dentro del bucle genera presión innecesaria sobre el GC?","Porque los strings son inmutables en C#: cada += crea un nuevo objeto string en el heap y descarta el anterior, generando una allocation por iteración",["Porque ToString() siempre lanza una excepción capturada internamente","Porque los bucles foreach no permiten modificar variables externas","Porque += en strings modifica el string original in-place sin crear uno nuevo"],2,"Como System.String es inmutable, cada concatenación produce un nuevo objeto en el heap; en un bucle con muchas iteraciones esto genera una cantidad significativa de basura para el GC, a diferencia de StringBuilder que reutiliza un buffer interno.",`string result = "";
foreach (var v in values) result += v.ToString() + ", "; // nueva allocation en cada iteración`),
  sqr("¿Qué es 'boxing' en C# y por qué genera una allocation en el heap?","Es la conversión implícita de un tipo por valor (como int, un struct) a object o a una interfaz, que requiere crear una copia envuelta ('boxed') en el heap administrado",["Es el proceso de compilar código C# a IL","Es la técnica de compresión de assemblies para reducir su tamaño en disco","Es el mecanismo que usa el GC para agrupar objetos del mismo tipo"],3,"Los value types viven normalmente en el stack o inline; al asignarlos a una variable de tipo object/interfaz (boxing), el runtime crea una copia en el heap, generando una allocation y presión adicional sobre el GC.",`object boxed = 42; // boxing: el int se copia al heap
int unboxed = (int)boxed; // unboxing: se copia de vuelta al stack`),
  sqr("¿Qué ventaja tiene StringBuilder frente a la concatenación repetida con += dentro de un bucle con muchas iteraciones?","StringBuilder mantiene un buffer interno mutable que reutiliza (redimensionándolo cuando es necesario), evitando crear un nuevo objeto string en cada operación de concatenación",["StringBuilder ejecuta el código en un hilo separado automáticamente","StringBuilder convierte el código en síncrono, eliminando la necesidad de await","StringBuilder elimina por completo la necesidad del Garbage Collector"],0,"A diferencia de la inmutabilidad de string, StringBuilder acumula caracteres en un buffer mutable interno, reduciendo drásticamente el número de allocations frente a concatenar strings repetidamente en un bucle."),
  sqr("¿Qué significa que el GC de .NET sea 'generacional'?","Que organiza los objetos en generaciones (Gen0, Gen1, Gen2) según cuánto tiempo llevan vivos, y recolecta las generaciones más jóvenes con más frecuencia porque es más probable que ya no estén en uso",["Que cada versión de .NET tiene un GC completamente distinto e incompatible","Que los objetos se numeran en el orden exacto en que fueron creados y se liberan en ese mismo orden","Que solo existe una única generación de objetos en el heap administrado"],1,"El diseño generacional se basa en la observación empírica de que la mayoría de los objetos mueren jóvenes; separar el heap en generaciones permite recolectar frecuentemente solo la porción más probable de contener basura (Gen0), en vez de escanear todo el heap cada vez."),
  sqr("¿Qué trampa de entrevista es común sobre 'usar struct en vez de class siempre reduce las allocations'?","Un struct grande copiado repetidamente por valor (por ejemplo, como parámetro o en una lista) puede generar más overhead de copia que una allocation ocasional en el heap; structs no son automáticamente 'gratis' en todos los escenarios",["Los structs nunca pueden causar ningún problema de rendimiento bajo ninguna circunstancia","Los structs siempre viven exclusivamente en el heap, igual que las clases","Convertir cualquier clase a struct mejora el rendimiento sin ninguna excepción"],2,"Aunque los structs evitan una allocation en el heap cuando permanecen en el stack, si son grandes y se copian con frecuencia (por valor, en colecciones, o mediante boxing si se usan como object), el costo de copiar puede superar al de una allocation puntual en el heap."),
  sqr("¿Qué relación tiene la fragmentación del Large Object Heap (LOH) con el rendimiento de una aplicación de larga duración?","Como el LOH tradicionalmente no se compacta en cada recolección, crear y liberar repetidamente objetos grandes puede dejar huecos de memoria no contigua, aumentando el uso de memoria y dificultando futuras allocations grandes",["El LOH nunca se fragmenta porque el GC lo compacta automáticamente en cada Gen0","La fragmentación del LOH solo afecta a aplicaciones de consola, no a servicios de larga duración","El LOH y Gen2 son heaps completamente independientes que nunca se recolectan juntos"],0,".NET permite compactar el LOH explícitamente (GCSettings.LargeObjectHeapCompactionMode), pero por defecto no siempre ocurre en cada recolección; asignar y liberar objetos grandes repetidamente en servicios de larga duración es un patrón conocido de degradación de memoria por fragmentación."),
  sqr("¿Qué acción reduce directamente la presión sobre el GC en un método que procesa una gran cantidad de elementos en un bucle?","Evitar crear objetos nuevos innecesarios dentro del bucle (como concatenar strings con +=, crear closures que capturan variables, o hacer boxing repetido de value types)",["Agregar más llamadas a GC.Collect() manualmente dentro del bucle","Convertir todo el método en async, sin ningún otro cambio","Aumentar el número de hilos que ejecutan el bucle en paralelo"],1,"Llamar a GC.Collect() manualmente rara vez ayuda (suele forzar una recolección completa costosa fuera de tiempo); la estrategia efectiva es reducir la cantidad de allocations generadas dentro de rutas de código calientes."),
  sqr("¿Por qué llamar explícitamente a GC.Collect() dentro de código de aplicación normal se considera generalmente una mala práctica?","Porque interrumpe la heurística del GC (que decide cuándo recolectar según la presión de memoria real) y suele forzar una recolección completa y costosa de todas las generaciones, incluyendo Gen2 y el LOH, en un momento subóptimo",["Porque GC.Collect() no existe como método público en .NET","Porque GC.Collect() solo puede llamarse una vez por proceso","Porque GC.Collect() libera automáticamente toda la memoria del sistema operativo"],2,"El GC ya decide de forma adaptativa cuándo y qué generación recolectar según la presión de memoria observada; forzar GC.Collect() manualmente casi siempre resulta en una recolección más costosa e innecesaria comparada con dejar que el runtime la programe."),
 ]},
 {id:"n08-span-memory",title:"Span<T> y Memory<T>",summary:"Span<T> representa una vista contigua y segura de memoria (de un array, string o memoria nativa) sin copiar los datos, permitiendo operaciones de slicing de alto rendimiento sin allocations adicionales. Es un ref struct: solo puede vivir en el stack, por lo que NO puede ser campo de una clase, ni capturarse en una lambda que genera un closure en el heap, ni usarse a través de un límite await en un método async (el compilador lo rechaza). Memory<T> resuelve esos casos: no es un ref struct, puede vivir en el heap y pasarse a través de código async, y expone .Span cuando se necesita acceso síncrono de alto rendimiento.",code:`// Span<T> evita copiar el array al extraer una porción (slice)
int[] numbers = { 1, 2, 3, 4, 5, 6 };
Span<int> slice = numbers.AsSpan(1, 3); // vista sobre {2, 3, 4}, sin copiar

// Esto NO compila: Span<T> no puede cruzar un punto 'await'
// public async Task ProcessAsync(Span<int> data) { await Task.Delay(1); ... }

// Memory<T> sí puede usarse en async porque no es un ref struct
public async Task ProcessAsync(Memory<int> data)
{
    await Task.Delay(1);
    Span<int> span = data.Span; // se convierte a Span solo para uso síncrono
    span[0] = 100;
}`,questions:[
  sqr("¿Qué representa Span<T> en .NET?","Una vista contigua y segura de memoria (de un array, string u otra región) que permite operar sobre ella sin copiar los datos subyacentes",["Una copia profunda de un array en un nuevo bloque de memoria","Un tipo de colección dinámica similar a List<T>","Un wrapper alrededor de Task<T> para operaciones asíncronas"],0,"Span<T> apunta directamente a una región de memoria existente (stack, array, memoria nativa) sin duplicarla, habilitando operaciones como slicing sin allocations adicionales."),
  sqr("¿Por qué Span<T> es un 'ref struct' en C#, y qué restricción práctica impone eso?","Es un ref struct porque solo puede vivir en el stack; por eso NO puede ser campo de una clase, ni almacenarse en el heap, ni cruzar un punto await dentro de un método async",["Ref struct significa que siempre se pasa por referencia a métodos, sin ninguna otra restricción","No impone ninguna restricción real, es solo una anotación informativa para el compilador","Significa que Span<T> solo puede usarse dentro de bucles for, nunca en otro contexto"],1,"La restricción de ref struct existe porque el compilador debe garantizar que Span<T> no sobreviva más que la memoria stack-allocated que podría estar referenciando; eso descarta usarlo como campo de clase, en closures que van al heap, o a través de un await."),
  sqr("¿Por qué el siguiente código NO compila: 'public async Task ProcessAsync(Span<int> data) { await Task.Delay(1); data[0] = 1; }'?","Porque Span<T> es un ref struct y no puede usarse en un método async cuando su valor debe persistir a través de un punto await (el compilador de C# lo rechaza explícitamente)",["Porque los métodos async no pueden recibir ningún parámetro","Porque Span<int> no tiene un indexador definido","Porque Task.Delay no puede llamarse dentro de un método que recibe parámetros por valor"],2,"El compilador prohíbe que un ref struct como Span<T> se mantenga 'vivo' a través de un await, porque el estado de una máquina de estados async podría necesitar moverse al heap, algo incompatible con las garantías de vida de un ref struct.",`public async Task ProcessAsync(Span<int> data)
{
    await Task.Delay(1);
    data[0] = 1; // error de compilación: Span<T> no puede cruzar un 'await'
}`),
  sqr("¿Qué tipo se usa en su lugar cuando se necesita pasar una porción de memoria a través de código asíncrono (por ejemplo, como parámetro de un método async)?","Memory<T>, que no es un ref struct y puede vivir en el heap, expone .Span para operaciones síncronas de alto rendimiento cuando se necesita",["Se debe usar array[] copiado manualmente en cada punto await","List<T>, convertida explícitamente en cada llamada async","No existe alternativa; Span<T> debe evitarse por completo en cualquier código async"],3,"Memory<T> es la contraparte de Span<T> pensada para escenarios donde la vista de memoria debe sobrevivir a través de código async o almacenarse como campo; su propiedad .Span la convierte a Span<T> solo para el tramo síncrono donde se necesita el mejor rendimiento."),
  sqr("En el ejemplo, ¿qué hace 'numbers.AsSpan(1, 3)' respecto al array original 'numbers'?","Crea una vista (Span<int>) sobre los elementos en las posiciones 1, 2 y 3 del array original, sin copiar ningún dato; modificar el Span modifica también el array subyacente",["Copia esos tres elementos a un nuevo array independiente","Elimina esos tres elementos del array original","Convierte el array completo en una lista enlazada"],0,"AsSpan(start, length) crea una vista sobre la misma memoria del array; como no hay copia, cualquier escritura a través del Span se refleja directamente en el array original, algo importante a tener en cuenta."),
  sqr("¿Puede Span<T> ser un campo de una clase (por ejemplo, 'private Span<int> _buffer;' dentro de una clase normal)?","No, porque al ser un ref struct, Span<T> solo puede vivir en el stack como variable local o parámetro, nunca como campo de un tipo por referencia que vive en el heap",["Sí, sin ninguna restricción, igual que cualquier otro campo","Sí, pero solo si la clase es 'sealed'","Sí, siempre que el campo se marque como 'readonly'"],1,"Un campo de una clase vive en el heap junto con la instancia; como un ref struct como Span<T> no puede residir en el heap, el compilador prohíbe declararlo como campo de una clase (aunque sí puede ser campo de otro ref struct)."),
  sqr("¿Qué ventaja de rendimiento ofrece 'string.AsSpan()' frente a usar Substring() para trabajar con una porción de un string?","AsSpan() crea una vista sobre el string original sin copiar caracteres; Substring() en cambio crea una nueva instancia de string con una copia de los caracteres, generando una allocation adicional",["AsSpan() siempre modifica el string original de forma destructiva, a diferencia de Substring()","No hay ninguna diferencia real de rendimiento entre ambos métodos","Substring() es más rápido porque no requiere calcular índices"],2,"Como los strings son inmutables, Substring() necesariamente copia los caracteres a una nueva instancia; ReadOnlySpan<char> obtenido con AsSpan() evita esa copia cuando solo se necesita leer o procesar una porción temporalmente."),
  sqr("¿Qué trampa de entrevista es común sobre intentar capturar un Span<T> dentro de una lambda que se convierte en un delegado almacenado (closure)?","El compilador lo rechaza si el closure necesita asignarse a un campo o vivir más allá del stack frame actual, porque el compilador generaría una clase para el closure que viviría en el heap, y un ref struct no puede ser campo de esa clase",["Siempre es válido capturar cualquier Span<T> en cualquier lambda sin ninguna restricción","Span<T> se convierte automáticamente en Memory<T> al capturarse en una lambda","Las lambdas en C# nunca pueden recibir ningún tipo de parámetro"],0,"Cuando una lambda captura variables y se convierte en un closure (una clase generada por el compilador), esas variables capturadas se vuelven campos de esa clase; como Span<T> no puede ser campo de un tipo heap-allocated, el compilador rechaza esa captura."),
  sqr("¿Qué gana ReadOnlySpan<T> frente a Span<T> cuando el código solo necesita leer datos, no modificarlos?","Expresa la intención de solo lectura en el sistema de tipos, evitando modificaciones accidentales del origen de datos, y permite crear vistas sobre datos inmutables como un string literal directamente",["ReadOnlySpan<T> es una clase, no un ref struct, por lo que puede vivir en el heap","ReadOnlySpan<T> es siempre más lento que Span<T> en cualquier escenario","ReadOnlySpan<T> convierte automáticamente cualquier código en asíncrono"],1,"ReadOnlySpan<T> comparte las mismas restricciones de ref struct que Span<T>, pero su indexador no permite escritura, comunicando la intención en la firma del método y permitiendo, por ejemplo, obtener una vista directamente sobre un string (que es inmutable)."),
  sqr("¿Qué caso de uso típico justifica preferir Span<T>/Memory<T> en código sensible al rendimiento, como parsing de texto o procesamiento de buffers de red?","Evitar allocations y copias intermedias al trabajar con porciones de un array/buffer/string existente, procesando los datos in-place en vez de crear nuevas instancias por cada operación de slicing",["Hacer que el código compile más rápido durante el build","Reemplazar por completo la necesidad de usar async/await en operaciones de I/O","Eliminar la necesidad de manejar excepciones en el código de parsing"],0,"Span<T> y Memory<T> son piezas clave del código de alto rendimiento en .NET (por ejemplo, en el parsing de System.Text.Json o en Kestrel) precisamente porque permiten operar sobre datos existentes sin las allocations que generarían Substring, ToArray, o copias manuales."),
 ]},
 {id:"n08-caching",title:"Caching",summary:"IMemoryCache guarda datos en memoria del propio proceso: es muy rápido pero no se comparte entre instancias y se pierde si el proceso se reinicia o si la app escala a varios servidores. IDistributedCache (implementado por ejemplo con Redis o SQL Server) comparte el mismo caché entre múltiples instancias de la aplicación y sobrevive a reinicios de una instancia individual, a cambio de latencia de red y necesidad de serializar los valores. La expiración puede ser absoluta (un instante fijo) o deslizante/sliding (se renueva con cada acceso), y elegir mal la estrategia de invalidación es una fuente común de bugs de datos obsoletos.",code:`public class ProductService
{
    private readonly IMemoryCache _cache;
    private readonly AppDbContext _db;
    public ProductService(IMemoryCache cache, AppDbContext db) { _cache = cache; _db = db; }

    public async Task<Product?> GetByIdAsync(int id)
    {
        return await _cache.GetOrCreateAsync($"product:{id}", async entry =>
        {
            entry.AbsoluteExpirationRelativeToNow = TimeSpan.FromMinutes(10);
            return await _db.Products.FindAsync(id);
        });
    }
}`,questions:[
  sqr("¿Cuál es la limitación principal de IMemoryCache en una aplicación desplegada con varias instancias detrás de un balanceador de carga?","Cada instancia mantiene su propio caché en memoria local, por lo que los valores cacheados no se comparten entre instancias y pueden quedar inconsistentes entre ellas",["IMemoryCache no permite establecer un tiempo de expiración","IMemoryCache no puede usarse dentro de servicios inyectados por DI","IMemoryCache requiere obligatoriamente una base de datos SQL configurada"],0,"Al vivir dentro del proceso de cada instancia, IMemoryCache no se sincroniza entre réplicas de la aplicación; una actualización cacheada en una instancia no se refleja en las demás."),
  sqr("¿Qué resuelve usar IDistributedCache (por ejemplo, respaldado por Redis) en lugar de IMemoryCache en un escenario con múltiples instancias?","Comparte el mismo caché entre todas las instancias de la aplicación, y el valor persiste aunque una instancia individual se reinicie, a costa de latencia de red y necesidad de serializar los datos",["Elimina por completo la necesidad de expiración de caché","Hace que las consultas a la base de datos sean innecesarias en cualquier escenario","Convierte automáticamente el caché en parte del código fuente"],1,"IDistributedCache centraliza el almacenamiento cacheado en un servicio externo compartido (Redis, SQL Server), resolviendo la inconsistencia entre instancias, aunque introduce latencia de red y overhead de serialización/deserialización."),
  sqr("En el ejemplo, ¿qué hace 'entry.AbsoluteExpirationRelativeToNow = TimeSpan.FromMinutes(10)' dentro de GetOrCreateAsync?","Configura que la entrada expire exactamente 10 minutos después de haberse creado, sin importar cuántas veces se acceda a ella en ese intervalo",["Renueva el tiempo de expiración cada vez que se lee la entrada del caché","Elimina la entrada inmediatamente después de crearla","Hace que la entrada nunca expire, permaneciendo indefinidamente en memoria"],2,"AbsoluteExpirationRelativeToNow fija un instante de expiración a partir del momento de creación; a diferencia de SlidingExpiration, no se renueva con los accesos posteriores."),
  sqr("¿Cuál es la diferencia entre expiración absoluta y expiración deslizante (sliding expiration) en un caché?","La expiración absoluta vence en un instante fijo desde la creación, sin importar el uso; la expiración deslizante se renueva cada vez que se accede a la entrada, extendiendo su vida mientras siga siendo consultada",["Son exactamente lo mismo, solo cambia el nombre de la propiedad usada","La expiración deslizante nunca vence, permanece para siempre en el caché","La expiración absoluta solo aplica a IDistributedCache, nunca a IMemoryCache"],3,"AbsoluteExpiration(RelativeToNow) fija un límite temporal fijo; SlidingExpiration reinicia el conteo de tiempo cada vez que la entrada se lee, útil para mantener 'caliente' un dato de uso frecuente pero permitiendo que expire si deja de consultarse."),
  sqr("¿Qué patrón implementa 'GetOrCreateAsync' del ejemplo respecto a la relación entre el caché y la base de datos?","El patrón cache-aside: primero intenta leer del caché, y si no existe, ejecuta la consulta a la base de datos y guarda el resultado en el caché para futuras lecturas",["El patrón write-through, donde cada escritura a la base de datos actualiza automáticamente el caché","Un patrón de reintentos con backoff exponencial","El patrón Unit of Work aplicado al caché"],0,"GetOrCreateAsync es la forma idiomática del patrón cache-aside en .NET: consulta el caché primero, y solo si falla (cache miss) ejecuta la función de carga (aquí, la consulta a la base de datos) y almacena el resultado."),
  sqr("¿Qué riesgo introduce cachear un producto por 10 minutos si su precio se actualiza directamente en la base de datos (por ejemplo, desde otro proceso) sin invalidar la entrada correspondiente del caché?","Los usuarios podrían ver el precio anterior (obsoleto/stale) durante hasta 10 minutos, hasta que la entrada expire y se recargue desde la base de datos",["Ningún riesgo, IMemoryCache detecta automáticamente cambios en la base de datos","La aplicación lanzaría una excepción inmediatamente al detectar la inconsistencia","El caché se invalidaría automáticamente en cuanto cambie cualquier fila de la base de datos"],1,"Sin invalidación explícita, un caché con expiración por tiempo puede servir datos obsoletos hasta que expire; si la frescura es crítica, hay que invalidar manualmente la entrada (por ejemplo, tras la actualización) además de o en vez de depender solo del tiempo de expiración."),
  sqr("¿Qué overhead adicional tiene IDistributedCache frente a IMemoryCache que no debe ignorarse al diseñar qué datos cachear?","Los valores deben serializarse (por ejemplo a JSON o binario) antes de enviarse al almacenamiento distribuido y deserializarse al leerlos, además de la latencia de red hacia el servicio externo (Redis, SQL Server)",["IDistributedCache no permite establecer ningún tiempo de expiración","IDistributedCache solo puede almacenar tipos numéricos, nunca objetos complejos","IDistributedCache es siempre más rápido que IMemoryCache en cualquier escenario"],0,"A diferencia de IMemoryCache (que guarda referencias a objetos .NET directamente en el mismo proceso), IDistributedCache trabaja con bytes/strings, lo que implica costo de serialización y latencia de red en cada lectura/escritura."),
  sqr("¿Qué trampa de entrevista es común sobre 'cachear todo agresivamente mejora siempre el rendimiento'?","Cachear datos que cambian con frecuencia, o con un tiempo de expiración mal calibrado, puede servir información obsoleta a los usuarios; el caching es un trade-off entre rendimiento y frescura de los datos, no una mejora automática y gratuita",["Cachear siempre es seguro sin ningún riesgo de mostrar datos desactualizados","El caching solo puede aplicarse a datos que nunca cambian, como constantes del lenguaje","Un caché más grande siempre es mejor sin ningún límite práctico de memoria"],2,"El diseño de una estrategia de caché exige decidir qué cachear, por cuánto tiempo, y cómo invalidar; ignorar la frescura de los datos para ganar velocidad puede introducir bugs sutiles de inconsistencia."),
  sqr("¿Qué ventaja tiene la clave usada en el ejemplo, '$\"product:{id}\"', frente a usar una clave genérica fija como \"product\" para todos los productos?","Permite cachear cada producto de forma independiente por su Id, evitando que la consulta de un producto sobrescriba o mezcle el valor cacheado de otro producto distinto",["No hay ninguna diferencia práctica entre ambos enfoques","Una clave con el Id hace que el caché consuma menos memoria en cualquier caso","Una clave genérica es siempre más eficiente porque hay menos entradas en el caché"],0,"Incluir el identificador del recurso en la clave del caché es esencial para que cada entrada represente un dato distinto; una clave compartida entre productos distintos causaría que todos los productos consultados devuelvan siempre el mismo valor cacheado erróneamente."),
  sqr("¿En qué escenario sería más apropiado IMemoryCache que IDistributedCache, a pesar de sus limitaciones de no compartirse entre instancias?","Cuando la aplicación corre en una sola instancia (o el dato cacheado es aceptable que difiera levemente entre instancias) y se prioriza la menor latencia posible, evitando el costo de red de un caché distribuido",["Siempre, IMemoryCache es preferible en cualquier escenario sin excepción","Nunca, IDistributedCache siempre debe usarse en cualquier aplicación en producción","Solo cuando la aplicación no usa ASP.NET Core"],1,"IMemoryCache ofrece la menor latencia posible al vivir en el mismo proceso; cuando no hay necesidad real de compartir el caché entre instancias (aplicación de una sola instancia, o datos tolerantes a pequeñas inconsistencias), puede ser la opción más simple y rápida."),
 ]},
 {id:"n08-efficient-async",title:"Async eficiente",summary:"Bloquear una operación asíncrona con .Result o .Wait() en vez de usar await puede provocar deadlocks en contextos con SynchronizationContext (por ejemplo, ASP.NET clásico o aplicaciones de escritorio), y siempre desperdicia un hilo del thread pool esperando de forma bloqueante, incluso donde no hay deadlock (como ASP.NET Core, que no tiene SynchronizationContext por defecto). Envolver trabajo que ya es asíncrono (I/O-bound) en Task.Run agrega overhead innecesario sin ningún beneficio real; Task.Run tiene sentido para desplazar trabajo CPU-bound fuera de un hilo que no debe bloquearse. ValueTask evita una allocation de Task en rutas calientes que suelen completarse de forma síncrona.",code:`// Mal: bloquea el hilo actual esperando una operación asíncrona
public string GetDataBlocking()
{
    return httpClient.GetStringAsync("https://api.com/data").Result; // riesgo de deadlock + hilo bloqueado
}

// Bien: propaga async de punta a punta
public async Task<string> GetDataAsync()
{
    return await httpClient.GetStringAsync("https://api.com/data");
}

// Mal: Task.Run innecesario envolviendo trabajo que ya es asíncrono (I/O-bound)
public Task<string> GetDataWrapped() => Task.Run(() => httpClient.GetStringAsync("https://api.com/data").Result);`,questions:[
  sqr("¿Qué riesgo específico introduce llamar a '.Result' sobre una Task en un contexto que tiene SynchronizationContext (como ASP.NET clásico o WPF/WinForms)?","Un deadlock: el hilo que bloquea con .Result puede ser el mismo hilo que el 'await' interno necesita para continuar, ya que ambos intentan usar el SynchronizationContext capturado",["Ninguno, .Result siempre es seguro de usar en cualquier contexto sin excepción","Una excepción de compilación inmediata","Una mejora automática de rendimiento al evitar la máquina de estados de async"],0,"El await interno de la operación asíncrona intenta volver al SynchronizationContext original para continuar; si ese contexto solo permite un hilo a la vez y ese hilo está bloqueado esperando con .Result, se produce un deadlock clásico."),
  sqr("¿Por qué bloquear con .Wait() o .Result desperdicia recursos incluso en ASP.NET Core, que no tiene SynchronizationContext por defecto (por lo que no hay deadlock)?","Porque el hilo que bloquea queda ocupado sin hacer trabajo útil mientras espera, reduciendo la cantidad de hilos del thread pool disponibles para atender otras peticiones concurrentes",["Porque ASP.NET Core prohíbe explícitamente compilar código que use .Result","Porque .Wait() siempre lanza una excepción en ASP.NET Core","Porque .Result convierte automáticamente el código en single-threaded para todo el proceso"],1,"Aunque sin SynchronizationContext no hay deadlock, un hilo bloqueado en .Result/.Wait() sigue consumiendo un hilo del thread pool sin liberar, lo cual bajo carga concurrente puede llevar a starvation del thread pool y degradar el throughput general."),
  sqr("En el código, ¿por qué 'GetDataWrapped()' que envuelve 'httpClient.GetStringAsync(...).Result' dentro de Task.Run NO es una solución razonable al problema de bloquear?","Porque sigue bloqueando un hilo (ahora del thread pool dentro de Task.Run) con .Result, solo que movió el bloqueo a otro hilo; además agrega el overhead de programar un Task.Run adicional sin resolver el problema real",["Porque Task.Run no puede usarse junto con GetStringAsync bajo ninguna circunstancia","Porque el compilador rechaza ese código con un error","Porque Task.Run siempre se ejecuta de forma síncrona, sin usar el thread pool"],0,"Mover el bloqueo dentro de Task.Run no elimina el bloqueo, solo lo reubica en otro hilo del thread pool; la solución correcta es usar await de punta a punta ('async todo el camino') en vez de mezclar código bloqueante con asíncrono.",`public Task<string> GetDataWrapped() => Task.Run(() => httpClient.GetStringAsync(url).Result); // sigue bloqueando, solo que en otro hilo`),
  sqr("¿Cuándo tiene sentido genuino usar Task.Run en código de servidor (por ejemplo, en un endpoint de ASP.NET Core)?","Para desplazar trabajo intensivo de CPU (CPU-bound) fuera de un contexto que no debería bloquearse, nunca para 'envolver' una operación que ya es asíncrona de I/O",["Siempre, es una buena práctica envolver cualquier llamada async con Task.Run","Nunca, Task.Run está prohibido en cualquier aplicación ASP.NET Core","Solo quando el método retorna void, nunca cuando retorna Task"],1,"Task.Run tiene un propósito legítimo: mover trabajo CPU-bound (cálculo pesado) a un hilo del thread pool para no bloquear el hilo actual; envolver una llamada I/O-bound que ya retorna Task (como GetStringAsync) no aporta ningún beneficio, ya que esa operación no bloquea ningún hilo mientras espera la red."),
  sqr("¿Qué es ValueTask<T> y en qué escenario reduce allocations frente a Task<T>?","Un tipo struct que puede representar un resultado ya disponible sin necesidad de asignar un objeto Task en el heap; útil en rutas calientes donde el resultado frecuentemente ya está disponible de forma síncrona (por ejemplo, un valor cacheado)",["Es idéntico a Task<T> en todos los aspectos, solo cambia el nombre por convención de estilo","Es un tipo exclusivo para operaciones que nunca pueden fallar","Es una versión de Task<T> que siempre se ejecuta en un hilo separado, nunca en el hilo actual"],0,"Task<T> es una clase (siempre requiere una allocation en el heap); ValueTask<T> es un struct que puede evitar esa allocation cuando el resultado ya está disponible sincrónicamente, útil en código de muy alto rendimiento con resultados frecuentemente cacheados o inmediatos."),
  sqr("¿Qué precaución importante debe tenerse al usar ValueTask<T>, a diferencia de Task<T>?","Un ValueTask<T> generalmente NO debe ser awaited más de una vez, ni almacenarse para usarlo después de forma concurrente; Task<T> sí soporta esos usos de forma segura",["ValueTask<T> nunca puede usarse con la palabra clave await","ValueTask<T> requiere obligatoriamente ConfigureAwait(false) en cada uso","ValueTask<T> solo puede declararse como campo estático de una clase"],1,"A diferencia de Task<T> (que es segura de awaited múltiples veces o compartida entre consumidores), ValueTask<T> tiene reglas de uso más estrictas por su implementación interna; usarla incorrectamente (doble await, cachearla) puede producir comportamiento indefinido."),
  sqr("¿Qué hace ConfigureAwait(false) al usarse después de un await, y en qué tipo de código se recomienda típicamente?","Indica que la continuación tras el await no necesita volver al SynchronizationContext/contexto original capturado, lo cual es especialmente relevante en código de bibliotecas reutilizables que no dependen de un contexto de UI específico",["Hace que la operación se ejecute de forma completamente síncrona, ignorando el await","Cancela automáticamente la operación si tarda demasiado tiempo","Convierte el método async en un método que retorna void en vez de Task"],2,"ConfigureAwait(false) evita el costo de volver al contexto de sincronización original tras el await; en bibliotecas que no necesitan ese contexto (por ejemplo, no tocan UI), reduce overhead y el riesgo de ciertos escenarios de deadlock si se usa en cadenas de llamadas que alguien más bloquea con .Result."),
  sqr("¿Qué trampa de entrevista es común sobre 'async void' en un método que maneja trabajo asíncrono?","Un método async void no puede ser 'awaited' por el llamador, por lo que sus excepciones no se propagan de forma capturable normalmente y pueden terminar el proceso; su único uso legítimo son manejadores de eventos, no lógica de negocio",["async void es siempre preferible a async Task por ser más simple de escribir","async void funciona exactamente igual que async Task en cualquier escenario","async void es obligatorio para manejar excepciones dentro del método"],0,"Como no hay una Task que representar, el llamador no puede esperar (await) ni capturar excepciones de un método async void de forma estándar; las excepciones no controladas ahí suelen propagarse directamente al SynchronizationContext y pueden derribar la aplicación."),
  sqr("¿Por qué 'propagar async de punta a punta' (async todo el camino, sin mezclar código síncrono bloqueante en medio) se considera la práctica recomendada?","Porque evita bloquear hilos innecesariamente en cualquier punto de la cadena de llamadas, previene deadlocks en contextos con SynchronizationContext, y permite que el runtime aproveche mejor el thread pool bajo carga concurrente",["Porque hace que el código compile más rápido","Porque elimina por completo la posibilidad de que ocurran excepciones","Porque convierte automáticamente el código en multihilo explícito"],0,"Mezclar código síncrono bloqueante (.Result, .Wait()) con código asíncrono anula los beneficios de async/await (liberar hilos mientras se espera I/O) y reintroduce los riesgos de deadlock y consumo innecesario de hilos que async/await busca evitar."),
  sqr("¿Qué efecto tiene sobre el rendimiento de un servidor con alta concurrencia tener varios endpoints que llaman '.Result' sobre llamadas HTTP salientes en vez de usar await?","Bajo carga, puede agotar el thread pool más rápido (thread pool starvation), aumentando la latencia general de la aplicación incluso para peticiones que no usan código bloqueante, porque hay menos hilos disponibles para atenderlas",["Ningún efecto medible, .NET compensa automáticamente cualquier bloqueo sin importar la carga","Mejora el rendimiento porque reduce el overhead de la máquina de estados de async/await","Solo afecta al hilo que hizo la llamada, sin ningún impacto en el resto de la aplicación"],1,"Cada hilo bloqueado con .Result reduce la capacidad efectiva del thread pool para atender nueva carga; bajo tráfico alto, esto puede degradar el rendimiento de toda la aplicación, no solo del endpoint que bloquea, por la contención por hilos disponibles."),
 ]},
 {id:"n08-benchmarkdotnet",title:"BenchmarkDotNet",summary:"Medir rendimiento con un Stopwatch manual en un bucle simple es engañoso: el JIT compila el método de forma progresiva (tiering), hay una fase de calentamiento (warmup) antes de alcanzar el código optimizado, y el GC puede interferir en momentos aleatorios, generando mediciones ruidosas y no reproducibles. BenchmarkDotNet es la biblioteca estándar en .NET para microbenchmarking: ejecuta automáticamente fases de pilot/warmup, múltiples iteraciones, y produce resultados con análisis estadístico (media, error, desviación estándar) que aíslan el código medido del ruido del entorno.",code:`[MemoryDiagnoser] // reporta también allocations por operación
public class StringConcatBenchmarks
{
    private readonly string[] _values = Enumerable.Range(0, 100).Select(i => i.ToString()).ToArray();

    [Benchmark(Baseline = true)]
    public string Concatenation()
    {
        string result = "";
        foreach (var v in _values) result += v;
        return result;
    }

    [Benchmark]
    public string WithStringBuilder()
    {
        var sb = new StringBuilder();
        foreach (var v in _values) sb.Append(v);
        return sb.ToString();
    }
}

// Se ejecuta con: BenchmarkRunner.Run<StringConcatBenchmarks>();`,questions:[
  sqr("¿Por qué medir el rendimiento de un método con un Stopwatch dentro de un simple bucle 'for' suele dar resultados poco confiables?","Porque no compensa el calentamiento del JIT (tiering, donde el código empieza sin optimizar y se recompila progresivamente), ni aísla el ruido de recolecciones del GC u otras variaciones del entorno durante la medición",["Porque Stopwatch no existe como clase en .NET","Porque los bucles for no pueden medirse bajo ninguna circunstancia","Porque Stopwatch siempre retorna 0 milisegundos en cualquier medición"],1,"Un benchmark ingenuo con Stopwatch mide una mezcla de código sin optimizar (antes de que el JIT tiering llegue al código totalmente optimizado), ruido del GC, y variabilidad del sistema, produciendo números que no reflejan el rendimiento real en estado estable."),
  sqr("¿Qué atributo de BenchmarkDotNet marca un método como el código cuyo rendimiento se quiere medir?","[Benchmark]",["[TestMethod]","[Measure]","[Profile]"],0,"El atributo [Benchmark] indica a BenchmarkDotNet que ese método debe ejecutarse repetidamente en un entorno controlado para medir su rendimiento."),
  sqr("En el ejemplo, ¿qué indica '[Benchmark(Baseline = true)]' sobre el método Concatenation()?","Que ese benchmark se usa como punto de referencia (1.00x) contra el cual BenchmarkDotNet compara el rendimiento relativo de los demás benchmarks de la clase (como WithStringBuilder)",["Que ese método se ejecuta solo una vez, sin repeticiones","Que ese método no puede lanzar excepciones durante la medición","Que ese método se excluye automáticamente del reporte final"],1,"Marcar un benchmark como Baseline permite que el reporte de BenchmarkDotNet muestre cuánto más rápido o lento es cada otro benchmark en relación proporcional a ese punto de referencia."),
  sqr("¿Qué información adicional aporta el atributo [MemoryDiagnoser] al reporte de BenchmarkDotNet?","La cantidad de memoria asignada (allocated) por cada operación medida, y el número de recolecciones de Gen0/Gen1/Gen2 provocadas durante el benchmark",["El tiempo exacto en el que ejecutó cada operación, expresado en fecha y hora del sistema","La cantidad de líneas de código de cada método medido","El nombre del hilo del sistema operativo que ejecutó cada benchmark"],2,"[MemoryDiagnoser] agrega columnas de 'Allocated' y de recolecciones de GC al reporte, permitiendo comparar no solo velocidad sino también presión de memoria entre distintas implementaciones (como Concatenation vs WithStringBuilder)."),
  sqr("¿Por qué BenchmarkDotNet ejecuta una fase de 'warmup' antes de tomar las mediciones que aparecen en el reporte final?","Para dar tiempo a que el JIT termine de recompilar el método al nivel de optimización más alto (tiered compilation) antes de medir, evitando que las primeras ejecuciones (más lentas, sin optimizar) contaminen el resultado",["Porque el disco duro necesita 'calentarse' físicamente antes de medir cualquier operación","Porque .NET requiere reiniciar el proceso antes de cada benchmark","Porque el warmup determina aleatoriamente qué benchmark se ejecuta primero"],0,"El JIT de .NET usa compilación por niveles (tiered compilation): al principio genera código rápido de compilar pero menos optimizado, y luego lo reemplaza por una versión más optimizada tras detectar uso frecuente; el warmup asegura medir el estado 'caliente' y estable del código."),
  sqr("¿Qué método se usa típicamente para ejecutar todos los benchmarks definidos en una clase con atributos [Benchmark]?","BenchmarkRunner.Run<T>()",["StringConcatBenchmarks.RunAll()","Stopwatch.StartNew().Run<T>()","Task.Run<T>().Benchmark()"],1,"BenchmarkRunner.Run<T>() es el punto de entrada estándar de BenchmarkDotNet: descubre los métodos marcados con [Benchmark] en la clase T y ejecuta el proceso completo de calentamiento, iteraciones y análisis estadístico."),
  sqr("¿Qué ventaja tiene ejecutar cada benchmark en un proceso separado (comportamiento por defecto de BenchmarkDotNet), en vez de correr todos los benchmarks dentro del mismo proceso de la aplicación?","Aísla el estado del runtime (JIT, GC, caches de CPU) entre benchmarks distintos, evitando que la ejecución de un benchmark contamine o sesgue la medición de otro benchmark posterior en el mismo proceso",["Reduce el tiempo total de ejecución de la suite de benchmarks a la mitad, siempre","Permite que los benchmarks se ejecuten en paralelo automáticamente","Elimina la necesidad de tener instalado el SDK de .NET"],0,"Ejecutar cada benchmark en su propio proceso limpio evita interferencia cruzada de estado acumulado (por ejemplo, generaciones del GC ya 'calientes' de un benchmark anterior) entre las distintas mediciones."),
  sqr("¿Qué trampa de entrevista es común sobre comparar el rendimiento de dos implementaciones 'a ojo', ejecutando cada una una sola vez y comparando el tiempo total con Console.WriteLine?","No cuenta con ninguna garantía estadística (varianza, desviación estándar, número de muestras); una sola ejecución puede estar dominada por ruido del sistema (otro proceso usando CPU, una recolección del GC puntual) y no reflejar el rendimiento típico real",["Es la forma más precisa y confiable de medir rendimiento en .NET","Console.WriteLine mide automáticamente el tiempo de ejecución sin necesidad de Stopwatch","Ejecutar una sola vez siempre da el mismo resultado que ejecutar mil veces"],0,"Una medición aislada y sin repetición está sujeta a variabilidad del entorno; BenchmarkDotNet resuelve esto ejecutando múltiples iteraciones y aplicando análisis estadístico para reportar un resultado representativo y su margen de error."),
  sqr("Si el reporte de BenchmarkDotNet muestra que WithStringBuilder es '5.2x' más rápido que Concatenation (el baseline) y asigna significativamente menos memoria, ¿qué relación tiene esto con lo visto en el paso de Allocations y GC?","Confirma empíricamente que la concatenación repetida de strings con += genera muchas más allocations (y por tanto más presión sobre el GC) que StringBuilder, que reutiliza un buffer interno, alineándose con la teoría de que menos allocations suele significar mejor rendimiento",["No tiene ninguna relación, son temas completamente independientes","Significa que StringBuilder no puede usarse junto con MemoryDiagnoser","Indica que el GC no participó en ninguna de las dos mediciones"],1,"BenchmarkDotNet con [MemoryDiagnoser] permite verificar de forma medible y reproducible las afirmaciones teóricas sobre allocations vistas anteriormente en el módulo, conectando la teoría del GC con datos concretos de un benchmark real."),
  sqr("¿Qué error de metodología se comete si se mide el rendimiento de un método directamente en modo Debug (dotnet run sin -c Release)?","El código en modo Debug generalmente no aplica las mismas optimizaciones del compilador/JIT que en modo Release, por lo que las mediciones no representan el rendimiento real que tendría la aplicación en producción",["Ninguno, Debug y Release generan exactamente el mismo código máquina","El modo Debug es siempre más rápido que Release porque incluye menos validaciones","BenchmarkDotNet no puede ejecutarse en absoluto si el proyecto está en modo Debug"],0,"BenchmarkDotNet de hecho detecta y advierte si se ejecuta un benchmark compilado en modo Debug, precisamente porque las optimizaciones deshabilitadas en Debug distorsionarían la medición respecto al comportamiento real en producción (Release)."),
 ]},
 {id:"n08-profiling",title:"Profiling",summary:"El profiling consiste en observar el comportamiento real de una aplicación en ejecución (CPU, memoria, I/O) para encontrar cuellos de botella reales, en vez de optimizar por intuición ('optimización prematura'). Herramientas como dotnet-trace capturan trazas de eventos de ETW/EventPipe para analizar dónde se gasta tiempo de CPU; dotnet-counters muestra contadores en vivo (uso de heap, tasa de excepciones, tamaño del thread pool); dotnet-dump y dotnet-gcdump capturan volcados de memoria para analizar allocations y posibles fugas. La regla general es medir primero, optimizar después, y volver a medir para confirmar la mejora.",code:`# Instalar las herramientas globales de diagnóstico de .NET
dotnet tool install --global dotnet-trace
dotnet tool install --global dotnet-counters
dotnet tool install --global dotnet-gcdump

# Ver contadores en vivo de un proceso en ejecución (identificado por su PID)
dotnet-counters monitor -p 1234

# Capturar una traza de CPU de 30 segundos para analizar después en un visor
dotnet-trace collect -p 1234 --duration 00:00:30

# Capturar un snapshot del heap administrado para analizar allocations
dotnet-gcdump collect -p 1234`,questions:[
  sqr("¿Qué principio general debería guiar cualquier esfuerzo de optimización de rendimiento?","Medir primero con herramientas de profiling para identificar el cuello de botella real, optimizar ese punto específico, y volver a medir para confirmar la mejora, en vez de optimizar por intuición",["Optimizar todo el código posible desde el principio, sin medir nada, para ahorrar tiempo","Reescribir la aplicación completa en cuanto se detecta cualquier lentitud percibida","Confiar exclusivamente en la opinión del desarrollador más experimentado del equipo, sin datos"],0,"La 'optimización prematura' basada en intuición frecuentemente enfoca esfuerzo en partes del código que no son el cuello de botella real; el profiling dirige la optimización hacia donde realmente importa, con datos objetivos."),
  sqr("¿Qué tipo de información captura principalmente dotnet-trace?","Trazas de eventos (basadas en EventPipe/ETW) para analizar dónde se gasta tiempo de CPU y qué rutas de código son las más costosas durante la ejecución",["Únicamente el contenido de los logs escritos con ILogger","El código fuente completo de los métodos ejecutados","Solamente las consultas SQL ejecutadas por Entity Framework"],1,"dotnet-trace recolecta eventos de diagnóstico del runtime (basados en EventPipe, la evolución multiplataforma de ETW) que permiten reconstruir en qué métodos y rutas de código se concentra el tiempo de CPU."),
  sqr("¿Qué diferencia hay entre dotnet-counters y dotnet-trace como herramientas de diagnóstico?","dotnet-counters muestra métricas agregadas en vivo (uso de heap, tasa de excepciones, hilos del thread pool) mientras la aplicación corre; dotnet-trace captura una traza detallada de eventos durante un periodo para analizar después con más profundidad",["Son exactamente la misma herramienta con dos nombres distintos","dotnet-counters solo funciona en Windows; dotnet-trace solo en Linux","dotnet-trace requiere reiniciar la aplicación; dotnet-counters no la afecta en absoluto"],0,"dotnet-counters es para monitoreo en tiempo real de indicadores agregados; dotnet-trace es para capturar y luego analizar en profundidad (por ejemplo con Perfview o Visual Studio) qué está ocurriendo exactamente durante un intervalo de ejecución."),
  sqr("¿Para qué se usaría dotnet-gcdump sobre un proceso en producción que se sospecha tiene una fuga de memoria (memory leak)?","Para capturar un snapshot del heap administrado y analizar qué objetos están vivos y cuántos de cada tipo, ayudando a identificar qué está reteniendo memoria innecesariamente",["Para reiniciar automáticamente el proceso y liberar toda la memoria usada","Para medir el tiempo de CPU consumido por cada método, sin relación con memoria","Para eliminar directamente los objetos no utilizados sin intervención del GC"],1,"dotnet-gcdump obtiene un volcado del heap administrado (grafos de objetos vivos y sus referencias), lo cual es la herramienta adecuada para diagnosticar por qué cierta memoria no se está liberando (por ejemplo, por referencias retenidas inesperadamente, como event handlers no desuscritos)."),
  sqr("¿Qué comando del ejemplo permite observar en vivo, sin capturar una traza completa, indicadores como el tamaño del heap o la cantidad de excepciones lanzadas por segundo mientras la aplicación está corriendo?","dotnet-counters monitor -p 1234",["dotnet-trace collect -p 1234","dotnet-gcdump collect -p 1234","dotnet build -c Release"],3,"dotnet-counters monitor -p <PID> conecta al proceso indicado y muestra en tiempo real contadores predefinidos (o personalizados) del runtime, útil como primer paso de diagnóstico antes de decidir si se necesita una traza más detallada."),
  sqr("¿Por qué es importante confirmar una mejora de rendimiento volviendo a medir con la misma herramienta y metodología después de aplicar una optimización?","Porque un cambio que parece lógico en teoría podría no tener el impacto esperado en la práctica (o incluso empeorar otro aspecto), y solo una medición posterior comparable confirma objetivamente que la optimización funcionó",["Porque .NET requiere una segunda medición obligatoria por licencia","Porque el compilador ignora cualquier optimización que no haya sido medida dos veces","Porque las herramientas de profiling solo funcionan si se ejecutan exactamente dos veces seguidas"],0,"El ciclo completo de optimización basada en evidencia es: medir, hipotetizar la causa, aplicar el cambio, y volver a medir bajo las mismas condiciones para verificar que la mejora es real y cuantificar su magnitud."),
  sqr("¿Qué trampa de entrevista es común sobre 'optimizar código que nunca se ejecuta en una ruta caliente (hot path)'?","Invertir tiempo optimizando un método que rara vez se ejecuta o cuyo costo es insignificante frente al tiempo total no mejora el rendimiento percibido de la aplicación; el profiling ayuda a enfocar el esfuerzo en las rutas que realmente dominan el tiempo total",["Cualquier optimización de código, sin importar dónde se aplique, siempre mejora el rendimiento general de forma proporcional","El profiling solo sirve para encontrar bugs de seguridad, no de rendimiento","Optimizar código poco usado siempre es más fácil y por eso siempre vale la pena priorizarlo"],0,"La ley de Amdahl y el sentido común del profiling coinciden: mejorar un fragmento de código que representa el 0.1% del tiempo total de ejecución tiene un impacto marginal, mientras que enfocar el esfuerzo en el cuello de botella real (identificado con datos) produce mejoras medibles."),
  sqr("¿Qué relación tiene BenchmarkDotNet (visto en el paso anterior) con el profiling de una aplicación en ejecución real?","BenchmarkDotNet mide con precisión el rendimiento de un fragmento de código aislado y controlado (microbenchmark); el profiling con dotnet-trace/dotnet-counters observa el comportamiento real de la aplicación completa en un entorno realista, incluyendo interacciones entre componentes que un microbenchmark no captura",["Son intercambiables y cualquiera reemplaza completamente al otro en cualquier escenario","BenchmarkDotNet reemplaza por completo la necesidad de dotnet-trace en producción","No tienen ninguna relación entre sí"],1,"Ambos son complementarios: BenchmarkDotNet ayuda a comparar alternativas de implementación de forma aislada y reproducible, mientras que el profiling en un entorno real (o similar a producción) revela dónde está el cuello de botella real dentro del sistema completo antes de decidir qué microbenchmark vale la pena escribir."),
  sqr("¿Qué significa que dotnet-trace y dotnet-counters puedan conectarse a un proceso .NET ya en ejecución mediante su PID, sin necesidad de reiniciarlo ni de instrumentar el código manualmente de antemano?","Que se basan en la infraestructura de diagnóstico integrada en el runtime de .NET (EventPipe), permitiendo diagnosticar aplicaciones en producción de forma no invasiva, sin agregar código de instrumentación manual ni detener el servicio",["Que requieren que el código de la aplicación llame explícitamente a una API de inicio de traza antes de poder conectarse","Que solo funcionan si la aplicación se ejecuta con privilegios de administrador en Windows","Que reinician automáticamente el proceso antes de comenzar a capturar datos"],0,"EventPipe es un mecanismo de diagnóstico multiplataforma incorporado en el runtime de .NET desde varias versiones atrás, que permite que herramientas externas como dotnet-trace y dotnet-counters se adjunten a un proceso en ejecución y recolecten datos sin necesidad de instrumentación manual previa en el código."),
  sqr("Si el profiling revela que la mayor parte del tiempo de CPU de un endpoint se gasta en 'Concatenation' (visto en el paso anterior sobre BenchmarkDotNet), ¿qué acción sería la más alineada con un enfoque basado en evidencia?","Reemplazar esa implementación específica por StringBuilder (u otra alternativa ya validada con un benchmark que demuestre la mejora) y volver a medir con las mismas herramientas para confirmar el impacto en el sistema real",["Reescribir toda la aplicación desde cero sin relación con el hallazgo específico del profiling","Ignorar el hallazgo porque el profiling nunca debe influir en decisiones de código","Aumentar el número de instancias del servidor sin cambiar ni una línea del código identificado como cuello de botella"],0,"El flujo correcto conecta el hallazgo del profiling (dónde está el costo real) con una intervención dirigida (el cambio de implementación específico, ya validado por separado con un microbenchmark) y una verificación posterior con la misma metodología de medición."),
 ]},
 {id:"n08-aot",title:"Compilación AOT",summary:"Native AOT (Ahead-Of-Time) compila la aplicación .NET directamente a código máquina nativo autocontenido durante la publicación, en vez de distribuir IL que el JIT compila en tiempo de ejecución. Esto produce arranque casi instantáneo y menor uso de memoria, a cambio de perder la flexibilidad del JIT: no hay compilación dinámica en tiempo de ejecución, la reflexión no confiable/no analizable estáticamente (como Reflection.Emit o algunos patrones de serialización basados en reflexión pura) no funciona, y el trimming agresivo puede eliminar código que solo se referencia dinámicamente si no se le indica explícitamente que lo preserve. Se publica con 'dotnet publish -r <RID> /p:PublishAot=true'.",code:`// Program.cs preparado para Native AOT: usa generadores de código en tiempo de compilación
// en vez de reflexión pura para serializar, evitando problemas de trimming.
[JsonSerializable(typeof(Producto))]
internal partial class AppJsonContext : JsonSerializerContext { }

var json = JsonSerializer.Serialize(producto, AppJsonContext.Default.Producto);

// Publicación Native AOT (línea de comandos, no C#):
// dotnet publish -c Release -r win-x64 /p:PublishAot=true`,questions:[
  sqr("¿Qué produce fundamentalmente la compilación Native AOT, a diferencia del modelo tradicional de .NET con JIT?","Código máquina nativo autocontenido generado durante la publicación, en vez de IL que el runtime compila justo a tiempo (JIT) cuando la aplicación arranca",["Un archivo de código fuente C# optimizado para recompilarse manualmente","Una versión reducida del SDK de .NET que se instala en el servidor destino","Bytecode compatible con la JVM de Java"],0,"Native AOT reemplaza el flujo habitual (compilar a IL, luego JIT en tiempo de ejecución) por una compilación completa a código nativo específico de la plataforma durante 'dotnet publish', antes de que la aplicación se ejecute."),
  sqr("¿Cuál es la ventaja principal que suele motivar el uso de Native AOT en servicios y funciones serverless?","Arranque casi instantáneo y menor huella de memoria, porque no hay que compilar IL a código nativo en tiempo de ejecución ni cargar el JIT",["Compatibilidad automática con cualquier código de reflexión sin ninguna restricción","Un tamaño de publicación siempre menor que framework-dependent en cualquier caso","Permite usar C# dinámico sin tipos, similar a JavaScript"],1,"Al eliminar el paso de JIT en el arranque, Native AOT reduce drásticamente el tiempo de inicio y el consumo de memoria inicial, lo cual es especialmente valioso en escenarios serverless o contenedores que escalan frecuentemente desde cero."),
  sqr("¿Qué limitación real impone Native AOT sobre el uso de reflexión dinámica no analizable estáticamente, como Reflection.Emit o ciertos patrones de serialización basados en reflexión pura?","No funciona correctamente, porque no hay JIT en tiempo de ejecución capaz de generar código dinámicamente; el compilador AOT necesita conocer de antemano, en tiempo de compilación, todo el código que podría ejecutarse",["No existe ninguna limitación real, toda la reflexión funciona exactamente igual que con JIT","Solo afecta a aplicaciones de consola, nunca a servicios web","Native AOT convierte automáticamente cualquier uso de reflexión en código equivalente sin advertencias"],2,"Como Native AOT compila todo por adelantado y no puede generar código nuevo en tiempo de ejecución, las APIs que dependen de emitir o cargar código dinámicamente (Reflection.Emit, ensamblados cargados dinámicamente, algunos usos de reflexión no analizables) no son compatibles o requieren alternativas basadas en generadores de código en tiempo de compilación."),
  sqr("En el ejemplo, ¿por qué se usa un 'JsonSerializerContext' generado con [JsonSerializable(typeof(Producto))] en vez de la serialización JSON basada puramente en reflexión?","Porque el generador de código de System.Text.Json en tiempo de compilación produce el código de serialización necesario de forma explícita y analizable estáticamente, evitando depender de reflexión dinámica que Native AOT (y el trimming) no puede garantizar que funcione correctamente",["Porque JsonSerializerContext es obligatorio en cualquier aplicación .NET, incluso sin Native AOT","Porque mejora la legibilidad del JSON generado para humanos","Porque reemplaza la necesidad de declarar la clase Producto"],0,"System.Text.Json ofrece 'source generators' que generan el código de (de)serialización en tiempo de compilación; esto es la forma recomendada de trabajar con JSON en Native AOT, ya que evita la reflexión dinámica que el trimming/AOT no puede validar de forma segura para tipos no referenciados explícitamente."),
  sqr("¿Qué es 'trimming' en el contexto de una publicación Native AOT, y qué riesgo conlleva si no se configura correctamente?","Es la eliminación del código (ensamblados, tipos, métodos) que el análisis estático determina que no se usa, para reducir el tamaño final; el riesgo es que elimine código que en realidad se necesita pero solo se referencia dinámicamente (por ejemplo, vía reflexión), causando fallos en tiempo de ejecución",["Es un proceso que solo afecta a los comentarios del código fuente, sin ningún efecto en el binario final","Es sinónimo exacto de minificación de archivos JavaScript","Es el proceso que valida automáticamente que no haya vulnerabilidades de seguridad"],1,"El trimmer analiza estáticamente qué código es alcanzable desde los puntos de entrada de la aplicación y descarta el resto; código alcanzado solo dinámicamente (reflexión no anotada, tipos usados solo por nombre en configuración) puede eliminarse por error si no se marca explícitamente para preservarse."),
  sqr("¿Qué comando de publicación habilita Native AOT para un proyecto .NET?","dotnet publish -c Release -r <RID> /p:PublishAot=true",["dotnet build --aot","dotnet run --native","dotnet new aot"],3,"PublishAot=true (junto con especificar un Runtime Identifier -r, ya que AOT genera código nativo específico de plataforma) es la forma estándar de habilitar la compilación Native AOT al publicar."),
  sqr("¿Qué relación hay entre Native AOT y una publicación 'self-contained' vista en el módulo 1?","Native AOT siempre produce una publicación self-contained (con el runtime necesario embebido en el binario nativo), ya que no depende de que un runtime .NET compartido esté instalado ni de un JIT en tiempo de ejecución",["Native AOT siempre requiere que el runtime de .NET esté preinstalado en el servidor destino, igual que framework-dependent","Son mutuamente excluyentes; nunca se puede combinar Native AOT con la idea de self-contained","Native AOT elimina la necesidad de un Runtime Identifier (RID) al publicar"],0,"Al compilar directamente a código nativo y no depender del CLR/JIT compartido del sistema, un ejecutable Native AOT es intrínsecamente autocontenido respecto al runtime, funcionando de forma similar (aunque técnicamente distinta) a self-contained en ese sentido."),
  sqr("¿Qué trampa de entrevista es común sobre 'Native AOT es simplemente una opción de compilación que siempre se puede activar sin cambios de código'?","En la práctica, código que depende fuertemente de reflexión dinámica, carga de ensamblados en tiempo de ejecución, o ciertos patrones de algunas bibliotecas puede requerir cambios (como usar generadores de código en vez de reflexión) para funcionar correctamente bajo Native AOT",["Native AOT es 100% compatible con cualquier código C# existente sin ninguna excepción ni advertencia","Native AOT solo puede aplicarse a proyectos ASP.NET Core, nunca a aplicaciones de consola","Activar Native AOT requiere reescribir la aplicación completa en otro lenguaje"],1,"Aunque .NET ha invertido en hacer Native AOT compatible con más escenarios (incluyendo ASP.NET Core mínimo), migrar una aplicación existente frecuentemente exige revisar y adaptar usos de reflexión, serialización y carga dinámica de ensamblados que no son compatibles con el modelo AOT."),
  sqr("¿Qué desventaja tiene Native AOT frente al modelo JIT tradicional en términos de flexibilidad en tiempo de ejecución?","Pierde la capacidad de generar o adaptar código dinámicamente durante la ejecución (por ejemplo, cargar plugins como ensamblados nuevos en tiempo de ejecución, o ciertos escenarios de compilación dinámica de expresiones), ya que todo el código debe conocerse y compilarse por adelantado",["Ninguna, Native AOT es estrictamente superior al JIT en todos los aspectos sin ninguna excepción","Native AOT hace que el código se ejecute más lento que con JIT en todos los escenarios","Native AOT no permite usar async/await en absoluto"],0,"El JIT permite escenarios dinámicos (cargar plugins, generar código en runtime, ciertos patrones de reflexión avanzada) que Native AOT no soporta, precisamente porque AOT exige que todo el grafo de código ejecutable se determine y compile en tiempo de publicación."),
  sqr("¿Para qué tipo de aplicación tiene MENOS sentido priorizar Native AOT como estrategia de despliegue?","Una aplicación que depende intensamente de un sistema de plugins cargados dinámicamente en tiempo de ejecución (ensamblados desconocidos en tiempo de compilación) o de reflexión extensa no analizable estáticamente",["Una función serverless que necesita arrancar en el menor tiempo posible ante cada invocación","Un microservicio en contenedor que escala horizontalmente con frecuencia y se beneficia de arranques rápidos","Una herramienta de línea de comandos (CLI) que se ejecuta muchas veces de forma breve"],0,"Native AOT brilla en escenarios de arranque rápido y bajo consumo de memoria (serverless, contenedores que escalan, CLIs), pero es una mala elección cuando la arquitectura depende fundamentalmente de cargar código desconocido en tiempo de ejecución, algo incompatible con su modelo de compilación cerrada por adelantado."),
 ]},
 {id:"n08-challenge",title:"Reto de performance",summary:"Reto integrador del módulo: combina generational GC y allocations, Span<T>/Memory<T>, caching, async eficiente, BenchmarkDotNet, profiling y Native AOT en el análisis y optimización de un método de procesamiento de datos con alto volumen de llamadas.",code:`// Versión inicial: allocations innecesarias y bloqueo síncrono de una llamada async
public string ParseAndFormat(string csvLine)
{
    var parts = csvLine.Split(','); // allocation de array + N strings
    var task = httpClient.GetStringAsync($"https://api.com/lookup/{parts[0]}");
    var lookup = task.Result; // bloquea el hilo, riesgo de deadlock/starvation
    return $"{parts[0]}-{lookup}";
}

// Versión optimizada: evita allocations con Span<T> y propaga async correctamente
public async Task<string> ParseAndFormatAsync(ReadOnlyMemory<char> csvLine)
{
    ReadOnlySpan<char> span = csvLine.Span;
    int commaIndex = span.IndexOf(',');
    ReadOnlySpan<char> id = span[..commaIndex]; // slice sin copiar

    var lookup = await httpClient.GetStringAsync($"https://api.com/lookup/{id}");
    return $"{id}-{lookup}";
}`,questions:[
  sqr("En la versión inicial de ParseAndFormat, ¿qué dos problemas de rendimiento vistos en este módulo aparecen simultáneamente?","'csvLine.Split(',')' genera allocations innecesarias (un array y varios strings nuevos), y 'task.Result' bloquea el hilo actual esperando una operación asíncrona, con riesgo de deadlock/starvation",["El método no tiene ningún problema real de rendimiento","Solo el uso de string interpolation es problemático, el resto del código es óptimo","El problema es que el método no está marcado como 'static'"],0,"Split() crea un nuevo array de strings (una allocation por cada substring encontrada), y bloquear con .Result mezcla código síncrono con asíncrono, dos antipatrones distintos vistos en los pasos de allocations/GC y async eficiente respectivamente."),
  sqr("¿Qué logra usar 'ReadOnlySpan<char> id = span[..commaIndex]' en la versión optimizada, en vez de 'csvLine.Split(',')[0]'?","Obtiene una vista sobre la porción del texto original hasta la primera coma, sin copiar caracteres a un nuevo string ni asignar un array intermedio, reduciendo las allocations frente a Split()",["Modifica el string original csvLine de forma destructiva","Ejecuta la operación de forma asíncrona automáticamente","Elimina por completo la necesidad de httpClient"],1,"El slicing con Span<T>/Memory<T> evita las allocations que produce Split() (un array de strings) y Substring, alineándose con lo visto en el paso de Span<T> y Memory<T> sobre evitar copias innecesarias."),
  sqr("¿Por qué ParseAndFormatAsync recibe el parámetro como 'ReadOnlyMemory<char> csvLine' en vez de 'ReadOnlySpan<char> csvLine', dado que el método es async?","Porque Span<T>/ReadOnlySpan<T> son ref struct y no pueden usarse como parámetro de un método async que cruza un punto await; Memory<T>/ReadOnlyMemory<T> sí pueden, y se convierten a Span solo en el tramo síncrono donde se necesitan (con .Span)",["Porque ReadOnlySpan<char> no existe como tipo en C#","Porque los métodos async no pueden recibir ningún tipo genérico como parámetro","Porque ReadOnlyMemory<char> es más rápido de leer que ReadOnlySpan<char> en cualquier escenario"],0,"Esto conecta directamente con la restricción vista en el paso de Span<T>/Memory<T>: como el método hace 'await httpClient.GetStringAsync(...)', no puede recibir un ref struct como parámetro persistente; se usa Memory<T> y se obtiene el Span solo para el procesamiento síncrono previo al await."),
  sqr("¿Qué cambio del código optimizado corrige el riesgo de deadlock/thread pool starvation presente en la versión inicial?","Usar 'await httpClient.GetStringAsync(...)' en vez de 'task.Result', propagando async de punta a punta en vez de bloquear el hilo actual esperando la respuesta",["Cambiar el nombre del método de ParseAndFormat a ParseAndFormatAsync, sin ningún otro cambio funcional","Usar ReadOnlyMemory<char> en vez de string como tipo de parámetro","Agregar el atributo [Benchmark] al método"],1,"El cambio funcionalmente crítico para el problema de bloqueo es reemplazar .Result por await; el cambio de tipo de parámetro (Memory<T> vs string) resuelve el problema de allocations, pero es un cambio distinto y complementario."),
  sqr("Si el equipo quiere confirmar con datos objetivos que ParseAndFormatAsync realmente asigna menos memoria que ParseAndFormat, ¿qué herramienta del módulo sería la más adecuada para esa comparación aislada y reproducible?","BenchmarkDotNet con el atributo [MemoryDiagnoser]",["dotnet-counters monitor, exclusivamente y sin ninguna otra herramienta","Console.WriteLine con Stopwatch dentro de un bucle simple","Native AOT, publicando la aplicación completa"],0,"BenchmarkDotNet con [MemoryDiagnoser] es la herramienta diseñada exactamente para comparar de forma controlada y estadísticamente confiable las allocations y el tiempo de dos implementaciones alternativas de un mismo método."),
  sqr("Si en producción se sospecha que el endpoint que usa ParseAndFormat está causando thread pool starvation bajo carga alta, ¿qué herramienta de profiling del módulo ayudaría a confirmarlo con datos en vivo?","dotnet-counters, que expone en tiempo real métricas como el tamaño del thread pool y la cola de trabajo pendiente",["dotnet-gcdump, exclusivamente, ya que starvation no tiene relación con el heap","BenchmarkDotNet, ejecutándolo directamente contra el proceso de producción","Native AOT, republicando la aplicación en caliente"],0,"dotnet-counters permite observar en vivo contadores relacionados con el thread pool (hilos activos, cola de trabajo) que son justamente los indicadores relevantes para diagnosticar starvation causada por bloqueos síncronos como .Result."),
  sqr("Si esta aplicación se publicara con Native AOT, ¿qué parte del código necesitaría revisión especial según lo visto en el paso anterior?","La interpolación de strings usada para construir la URL ($\"https://api.com/lookup/{id}\") es simple y compatible, pero si el resultado de la llamada HTTP se deserializara con JSON basado en reflexión pura (sin un JsonSerializerContext generado), eso sí requeriría adaptarse para funcionar de forma confiable bajo trimming/AOT",["Ninguna parte, Native AOT es siempre 100% compatible con cualquier código sin ninguna excepción","Todo el método debería reescribirse en otro lenguaje de programación","El uso de 'await' dejaría de funcionar completamente bajo Native AOT"],1,"Aunque el ejemplo no deserializa JSON directamente, es el punto de extensión natural donde aplicaría la lección de AOT: cualquier (de)serialización basada en reflexión pura en este flujo debería migrarse a generadores de código en tiempo de compilación para ser segura bajo trimming/Native AOT."),
  sqr("¿Qué principio general, común a Span<T>/Memory<T>, caching y async eficiente, conecta las optimizaciones aplicadas en ParseAndFormatAsync?","Evitar trabajo innecesario y desperdiciado: allocations que no aportan valor, bloqueos de hilos que podrían liberarse mientras se espera I/O, y (con caching) llamadas repetidas a un recurso costoso cuando el resultado podría reutilizarse",["Ejecutar siempre el código en paralelo usando la mayor cantidad de hilos posible","Reemplazar todo el código síncrono por Native AOT sin excepción","Aumentar el tamaño de todos los buffers usados en la aplicación al máximo posible"],0,"El hilo conductor del módulo es reducir trabajo innecesario en las rutas calientes del código: menos allocations (GC, Span/Memory), menos trabajo repetido (caching), y no desperdiciar hilos bloqueando esperas que podrían ser asíncronas (async eficiente)."),
  sqr("Si se agregara un IMemoryCache para evitar llamar a httpClient.GetStringAsync repetidamente con el mismo 'id' en un corto periodo, ¿qué riesgo del paso de Caching habría que considerar?","Que el resultado cacheado quede obsoleto si el valor remoto (el 'lookup') cambia durante el tiempo de expiración configurado, sirviendo una respuesta desactualizada hasta que la entrada expire",["Ningún riesgo, cachear siempre es una mejora estrictamente positiva sin ninguna contrapartida","El caché eliminaría automáticamente la necesidad de usar async/await en el método","IMemoryCache no puede usarse junto con ReadOnlyMemory<char> bajo ninguna circunstancia"],0,"Como se vio en el paso de Caching, cualquier caché con expiración por tiempo introduce una ventana donde el dato puede estar obsoleto; esa decisión de diseño (cuánto tiempo cachear, si invalidar activamente) debe tomarse conscientemente, no como una mejora gratuita sin trade-offs."),
  sqr("¿Qué combinación de conceptos del módulo se refleja mejor en el flujo completo del reto (slicing con Span/Memory + await en vez de .Result + posible verificación con BenchmarkDotNet y profiling)?","Allocations y GC (menos objetos creados) + Span<T>/Memory<T> (slicing sin copias) + async eficiente (no bloquear hilos) + BenchmarkDotNet y profiling (medir para confirmar la mejora con datos reales)",["Solo Native AOT, el resto de conceptos no participan en este código","Solo caching, ya que el método llama a un servicio HTTP externo","Ninguno de los conceptos del módulo, es código genérico sin relación con ellos"],0,"El ejemplo del reto combina deliberadamente varias piezas del módulo: reducir allocations con slicing, evitar bloqueo de hilos con async correcto, y validar cualquier mejora con las herramientas de medición (BenchmarkDotNet, profiling) en vez de asumir que el cambio es mejor sin comprobarlo."),
 ]},
];
